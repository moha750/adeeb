# مراجعة الهوية البصرية — لوحة تحكم أديب (V2)

> مراجعة خبير شاملة للهوية البصرية **كما هي مطبَّقة فعلًا** في لوحة V2.
> الغرض: توثيق اللغة البصرية القائمة (فهي موجودة وكاملة)، وتقييمها، وكشف مواضع الانحراف بالأدلّة — لتصير هذه الوثيقة المرجع المُقنَّن الواحد.
> المنهج: قراءة كاملة لـ `tokens.css` + `globals.css` (2718 سطرًا) + الخطوط والـpreset + الهيكل والمكوّنات. آخر تحديث: 2026-07-08.

---

## 0) الحكم العام

**اللوحة تحمل هوية بصرية ناضجة ومتماسكة الحسّ — لكنها غير مُقنَّنة.** هناك لغة بصرية واضحة يتعرّفها العين فورًا: تدرّجات كحلي/فولاذي زاويّة، أسطح «Aurora» ناعمة بظلال فولاذية ملوّنة، شريط جانبيّ كحليّ عائم، طباعة Lyon مع أرقام Eras، وحركة غنيّة موسومة. المشكلة ليست غياب الهوية بل أن **قيمها الفعلية تعيش كوصفات منسوخة في `globals.css` لا كرموز مُسمّاة في `tokens.css`** — فتتذبذب القيمة نفسها من مكوّن لآخر، ويتعايش «جيلان» من الألوان الدلالية معًا.

**الخلاصة:** الهوية لا تحتاج إعادة تصميم — تحتاج **تقنينًا وتوحيدًا**: رفع الوصفات المتكررة إلى رموز، وحسم القيمة الواحدة لكل لون، وفرض القاعدة.

---

## 1) الأسس المعتمدة (العلامة الرسمية)

| العنصر | القيمة | الحالة |
|--------|--------|--------|
| اللون الأساسي | كحلي `#274060` (‏`--navy-700`‏) | ✅ مطبَّق |
| اللون الثانوي | فولاذي `#335C81` (‏`--steel-600`‏) | ✅ مطبَّق |
| خط العربية/العناوين | Lyon Arabic (5 أوزان) | ✅ مطبَّق |
| خط اللاتيني/الأرقام | Eras (4 أوزان) | ✅ مطبَّق |
| الشعار (SVG أفقي/عمودي + أبيض) | في `public/brand/` و`packages/design-system/brand/` | ⚠️ موجود لكن **غير مستخدَم في اللوحة** |
| الباترن (خطّي/دائري + أبيض) | SVG | ✅ مستخدَم (ترويسة كرت العضو `pattern-white.svg`) |
| الاتجاه واللغة | `<html lang="ar" dir="rtl">` — `layout.tsx:16` | ✅ سليم |

القاعدة المقدّسة المطبَّقة: **اللونان لا ينفصلان** — يظهران دائمًا معًا كتدرّج زاويّ 135° (`--grad-primary`)، لا كحليًّا صلبًا منفردًا. هذا محقَّق في الأزرار والشارات والـAvatar والترقيم والتبويبات.

---

## 2) نظام اللون كما هو مطبَّق

### 2‑أ) المقاييس الأساسية — منضبطة ✅
`tokens.css` يعرّف مقاييس كاملة ومحكمة: `--navy-50…950` و`--steel-50…900` و`--neutral-50…900`، مبنيّة حول لوني العلامة. هذه سليمة ولا تحتاج تعديلًا.

### 2‑ب) تدرّجات الهوية — مُرمَّزة ✅
```
--grad-primary        : linear-gradient(135deg, steel-500, navy-700)
--grad-primary-strong : linear-gradient(135deg, steel-400, navy-800)
--grad-primary-hover  : linear-gradient(135deg, steel-600, navy-800)
```
هذه هي «تعبئة العلامة» المعتمدة، ومتوفّرة كأدوات `.bg-brand` / `.bg-brand-strong`. مطبَّقة باتّساق جيّد.

### 2‑ج) الألوان الدلالية — **جيلان متعايشان (أكبر مصدر للتخبّط)** ⚠️
يوجد فعليًّا **مجموعتان لونيّتان دلاليّتان** حيّتان معًا، ولكلّ لون 2–3 قيَم مختلفة حسب المكوّن:

| المعنى | «الهادئة» (رموز `tokens.css`) | «الأنصع» (المعتمدة فعلًا في المكوّنات) | قيمة ثالثة (الحقول) |
|--------|:---:|:---:|:---:|
| نجاح | `#2e8b6b` (`--success`) | **`#1f9d5f`** (شارات/عدّاد/تنبيه/نقطة Avatar) | `#1f8a5b` (`.af-success`) |
| تحذير | `#c58a2c` (`--warning`) | **`#d9920f`** (شارات/عدّاد/تنبيه) | — |
| خطأ | `#c0473c` (`--danger`) | **`#d23b2c`** (شارات/عدّاد/تنبيه/توست/كرت موقوف) | `#c0392b` (`.af-error`) |

الأدلّة: `tokens.css:28-31` مقابل `globals.css:1239-1241` (شارات) و`1285-1287` (عدّاد) و`1376-1378` (تنبيهات) مقابل `1027,1037` (الحقول).

**تناقض داخل المكوّن الواحد:** التوست الناجح خلفيته `var(--success)` (‏`#2e8b6b`‏ الهادئ) وأيقونته `var(--grad-success)` (‏`#1f9d5f`‏ الأنصع) — أخضران في مكوّن واحد (`globals.css:2456-2457`).

**القرار المطلوب (تقني لا ذوقي):** تُعتمد **الأنصع** كقيمة واحدة (لأنها المهيمنة فعلًا في كل المكوّنات المرئية)، وتُحدَّث رموز `--success/-warning/-danger` إليها، ويُحذف `#1f8a5b`/`#c0392b`. النتيجة: «أخضر أديب» واحد، «أحمر أديب» واحد.

### 2‑د) الأسطح والثيم — منضبطة ✅
`--color-bg / surface / surface-2 / text / text-muted / border` تنقلب صحيحًا بين فاتح/داكن/يدوي (`tokens.css:99-130`). ممتاز.

### 2‑هـ) لوحة «فوق الداكن» — **غير مُرمَّزة** ⚠️
الشريط الجانبيّ الكحليّ يحتاج ألوان نصّ فاتحة، لكن **لا يوجد مقياس on-dark في الرموز**، فاختُرعت ~14 درجة موضعيًّا:
`#c7d3e2` (نصّ الشريط)، `#c1cede` (عنصر تنقّل)، `#93a4ba` (عنصر فرعي)، `#8fa1b8` (ترويسة مجموعة)، `#eaf0f6` (تمرير فرعي)، `#dbe4ee` (التذييل)، `#e3ecf5` (شارة)، `#dbe6f2` (أيقونة التذييل) — إضافة إلى توهّجات `rgba(111,151,192,…)`.
**التوصية:** مقياس `--on-dark-1…4` (نصّ/خافت/أخفت/فاصل) في الرموز.

### 2‑و) الحُجب الداكنة (Scrims) — أربع درجات متقاربة غير مُرمَّزة ⚠️
`rgba(20,31,48,.45)` (درج الجوال)، `rgba(20,31,48,.42)` (حجاب النافذة)، `rgba(15,22,33,…)` (تدرّجات الصور)، `rgba(18,30,46,.5)` (شريط الكاروسيل). **التوصية:** رمز `--scrim` واحد.

---

## 3) لغة السطح «Aurora» — الموتيف المميِّز للّوحة

هذه **أبرز سمة بصرية للّوحة** وأنجحها حسًّا: كل بطاقة/جدول/قائمة/نافذة سطحها ليس أبيض مسطّحًا بل **تدرّج فولاذيّ ناعم + حدّ ممزوج + ظلّ فولاذيّ ملوّن**. الوصفة القانونية (كما في `.acard`، `globals.css:1296-1301`):

```css
background: linear-gradient(135deg, color-mix(in oklab, #335c81 12%, surface), surface 62%);
border:     1px solid color-mix(in oklab, #335c81 10%, --color-border);
box-shadow: 0 16px 36px -20px rgba(51,92,129,.42);
```

**أين تظهر:** `.acard` · `.mc` (كرت العضو) · `.tb` (الشريط) · `.tb-fs-panel` · `.dm-menu` · `.pv-hero` · `.pv-card` · `.gal-sec` · `.gal-toc` · `.aalert` (بلون النغمة) · `.ach-card`.

**التقييم:** الموتيف موحّد الحسّ لكنه **منسوخ لا مُرمَّز**، فتذبذبت تفاصيله:
- **القاعدة اللونية منقسمة:** أحيانًا حرفيًّا `#335c81`، وأحيانًا `var(--steel-500)` (‏`#45719a`‏)، وأحيانًا `var(--steel-600)`.
- **نسبة المزج تتذبذب:** ‎7% / 8% / 9% / 10% / 11% / 12% / 15%‎ عبر المكوّنات.
- **كرت العضو ينسخ الوصفة «حرفيًّا»** من `.acard` (تعليق صريح في `globals.css:2527`)، بدل إعادة استخدام `.acard`.

**التوصية:** تقنينه كرمز/أداة واحدة — مثل `--surface-aurora` و`--border-aurora` و`--shadow-aurora` (أو `.bg-aurora`) بقاعدة `steel-600` ونسبة واحدة (‏~12%‎) وسلّم ارتفاع (سطح/مرتفع). هذا وحده يُنهي ثاني أكبر مصدر للتذبذب.

---

## 4) الطباعة كما هي مطبَّقة

- **الخطّان صحيحان:** Lyon Arabic (عربي/عناوين) + Eras (لاتيني)، بلا خطوط نظام. `fonts.css` تعريف واحد نظيف بخمسة أوزان Lyon وأربعة Eras.
- **حيلة ذكيّة تستحقّ الإبقاء:** الأرقام (`U+0030-0039`) مُعاد تعيينها لخطّ Eras داخل عائلة «Lyon Arabic» عبر `unicode-range` (`fonts.css:20-24`) — فتظهر الأرقام بـEras تلقائيًّا وسط النصّ العربي بلا تدخّل. أنيق.
- **الأدوات:** `.lat` و`.num` تفرضان Eras صراحةً للبريد/الأرقام خارج الجدول.

**الانحراف:** ⚠️
- **سلّم الأحجام (`--text-xs…6xl`) شبه مُهمَل** في `globals.css`؛ الأحجام مكتوبة بالبكسل حرفيًّا وبكثرة، بما فيها أنصاف بكسل: `14.5px`, `13.5px`, `16.5px`, `12.5px`, `11.5px`… عشرات المواضع.
- **سلّم الأوزان (`--fw-*`) غير مستعمل**؛ الأوزان `600/700/800/900` مكتوبة رقمًا مباشرة.
**التوصية:** سلّم طباعة مُسمّى (`display/heading/title/body/caption` + أوزان) يُستهلك بدل البكسل الحرفي.

---

## 5) الشكل والمسافة

### الاستدارة (radius) — **السلّم شبه ميّت** ⚠️
`--radius-sm/md/lg/xl = 6/10/14/20` معرّف، لكن `var(--radius-*)` نادر الاستعمال مقابل ~90 قيمة `border-radius` حرفية. والقيم المستعملة فعلًا **خارج المقياس غالبًا**:

| العنصر | الاستدارة الفعلية | على المقياس؟ |
|--------|:---:|:---:|
| الزرّ `.abtn` | `16px` | ❌ (بين md=10 وxl=20) |
| البطاقة `.acard`/`.mc` | `18px` | ❌ |
| الهيكل (`ash-main`/`ash-side`/`mdl`) | `18px` | ❌ |
| الـAvatar | `16px` («مطابقة للأزرار» — تعليق `globals.css:2235`) | ❌ |
| الحقل/Select | `10px` | ✅ md |
| القوائم | `12px` / `9px` / `8px` | ❌ |

يوجد **نظام زوايا فعليّ** (زرّ 16 · بطاقة 18 · حقل 10) لكنه غير مطابق للرموز. **التوصية:** توسيع السلّم ليعتمد القيم المستعملة فعلًا (‏`--radius: 8/10/12/16/18/20`‏) ثم استبدال كل رقم حرفيّ برمز.

### المسافات
سلّم `--space-*` (أساس 4px) معرّف وسليم، لكن الحشوات في `globals.css` مكتوبة بالبكسل غالبًا. أقلّ إلحاحًا من اللون والاستدارة، لكنه ضمن التوحيد نفسه.

---

## 6) الحركة والعمق — هوية غنيّة موسومة ✅ (مع تذبذب طفيف)

**رموز الحركة:** `--ease-standard: cubic-bezier(.2,.6,.2,1)` و`--dur-fast/.15` و`--dur/.25` و`--dur-slow/.4` — معرّفة ومستعملة في الهيكل والجدول والقوائم.

**التوقيعات الحركيّة المميِّزة (تُبقى — هي جزء من شخصية اللوحة):**
- **كنس ضوئيّ** على الأزرار عند المرور (`.abtn::after`، انزلاقة 1s).
- **نبض حيّ** للشارة (`.abadge-live` ping).
- **لمعان منزلق** للهيكل التحميلي (`.sk` shimmer).
- **ارتداد زنبركيّ** لعناصر الاختيار (`cubic-bezier(0.2,1.9,0.35,1)`).
- **دخول التوست** من الأسفل + شريط تقدّم متناقص.
- **ظهور الأقسام عند التمرير** (`[data-reveal]`)، والجدار الحيّ للأعمال، وكاروسيل المجلس، والعرض التحريريّ للأخبار.
- **احترام `prefers-reduced-motion`** في كل مكان تقريبًا — ممتاز.

**الظلال — «ارتفاع ملوّن» غير مُرمَّز:** ⚠️ رموز `--shadow-sm/md/lg` محايدة (‏`rgba(22,38,58)`‏)، لكن أغلب المكوّنات تستعمل ظلالًا **فولاذية ملوّنة** حرفيًّا: `rgba(51,92,129,.42/.5/.6/.7)`. هذا في الحقيقة **سمة هوية جميلة** (العمق بلون العلامة لا رمادي)، لكنها تستحقّ أن تُرمَّز (‏`--shadow-brand-sm/md/lg`‏) بدل تكرارها بنسب متذبذبة.

**التذبذب:** إلى جانب الرموز، تُكتب مدد حرفية (`0.2s`, `0.3s`, `0.55s`, `0.7s`) — توحيدها ضمن السلّم مفيد.

---

## 7) الهيكل (Shell) — `.ash-*`

**التخطيط:** «عائم زجاجيّ» — الشريط والمحتوى بطاقتان مستديرتان (18px) بينهما فراغ 14px، على خلفية بتوهّج فولاذيّ قطريّ (`globals.css:1458`).

**الشريط الجانبيّ:**
- **كحليّ دائمًا** (لا يتبع الثيم) — `linear-gradient(160deg, steel-700, navy-900)` + توهّج فولاذيّ (`globals.css:1478-1480`). قرار مقصود («الهوية لا تختفي»).
- يُطوى إلى **ريّل أيقونات 71px** عبر مقبض دائريّ على حدّ الشريط (لا في الهيدر) — مطابق لقاعدتك.
- **الشعار = حرف «أ»** في مربّع متدرّج + «لوحة أديب» (`DashboardShell.tsx:72-74`). ⚠️ **الشعار الرسمي (SVG) غير مستخدَم** رغم توفّره — أبرز فجوة هوية في اللوحة.

**الهيدر:**
- **الهوية دائمة الظهور:** Avatar + ترحيب زمنيّ + التاريخ (`ash-greet`) — مطبَّق كقاعدة مقدّسة حتى على الجوال (يبقى الاسم والصورة، ويتقلّص البحث وحده). ✅
- بحث + جرس بإشعار.

**الجوال:** الشريط يصير درجًا منزلقًا مع حجاب. سليم.

**ملاحظة نظافة:** بقايا تعليقات «إعادة تصريف» بطوابع زمنية وقرارات متناقضة متتالية في ذيل `globals.css:2469-2671` (`suspended red header` ← `lighter red` ← `from --danger token` ← `revert…`) — أثر التطوير اليدويّ بالتجربة؛ تُنظَّف.

---

## 8) جرد المكوّنات وهويّتها البصرية

كل الأنماط في `globals.css` ببادئات متّسقة (تُسهّل القراءة). الجرد:

| المكوّن | البادئة | الهوية البصرية | ملاحظات المراجعة |
|--------|--------|----------------|------------------|
| زرّ | `.abtn` | تدرّج + كنس ضوئي + سبينر · زوايا 16 | **13 نمطًا** (primary/ghost/danger/success/warning + ghost‑* + inverse + inverse‑*) — سطح أنماط واسع جدًّا |
| حقل | `.af-` | تسمية عائمة · غور داخلي · حلقة تركيز فولاذية | حالات النجاح/الخطأ بلونين خارج الرموز |
| منسدل | `.asel` | زناد بهوية الحقل · سهم في دائرة يدور · بحث+تجميع | متين |
| اختيار | `.ach` | checkbox/radio/switch/card · ارتداد زنبركي | يُنسخ ترميزه يدويًّا في خانة اختيار الجدول |
| شارة/عدّاد | `.abadge`/`.acount` | 5 نغمات · soft/solid/outline/subtle/glass · نبض حيّ | مصدر الألوان «الأنصع» |
| بطاقة | `.acard` | **سطح Aurora** · media/body/footer/chip | الوصفة القانونية للسطح |
| تنبيه | `.aalert` | Aurora بلون النغمة (`--ac`) | أيقونة مؤطّرة + إجراءات |
| جدول | `.dt` | شبكة مؤطّرة · تناوب فولاذي 4.5% · فرز · خلية عضو | ناضج |
| شريط أدوات | `.tb` | Aurora · بحث/مرشّحات/تحديد جماعي | Aurora بنسبة 9% |
| قائمة إجراءات | `.dm` | Radix · قائمة Aurora · خطر | سلوك ممتاز |
| نافذة | `.mdl` | Radix · حجاب مضبَّب · رأس/جسم/تذييل | أحجام sm/md/lg |
| ترقيم | `.pag` | رقم نشط = تدرّج · أسهم | متّسق |
| Avatar | `.av` | مربّع مستدير (16) · تدرّج · نقطة حالة متدرّجة | Lyon للأحرف العربية |
| تبويبات | `.tabs` | underline/pill/segmented | نمط segmented للأزرار داخل النافذة مكرّر (`.mdl-seg`) |
| حالة فارغة | `.empty` | soft/aurora | متّسق |
| هيكل تحميل | `.sk` | لمعان منزلق | جيّد |
| توست | `.toast` | Aurora بلون النغمة · أيقونة مربّعة · تقدّم | الأخضران داخله (§2‑ج) |
| كرت العضو | `.mc` | سطح Aurora · **ترويسة بباترن العلامة** · Avatar متراكب · شارة دور | ينسخ `.acard` حرفيًّا بدل إعادة استخدامه |
| معرض المكوّنات | `.gal` | سقالة عرض Aurora مرقّمة | `/dashboard/components` — لوح مراجعة حيّ |

**أين تُستهلَك المكوّنات فعلًا (من طبقة TSX):** الشاشات تستورد من الحزمة `Badge/Button/Field/Select/Alert/Card` فقط؛ أمّا العُدّة التفاعلية الأنضج (`Avatar/DataTable/DropdownMenu/Modal/Pagination/Skeleton/Tabs/Toolbar/Toast`) فمحلّية في `dashboard/_components`. **بطاقة الحزمة `Card` تُستعمل في «نظرة عامة» والمعرض فقط**؛ كل أسطح الأعضاء (الكرت، الجدول، الملف) مبنيّة من مكوّنات محلّية + ماركب مُنمَّط يدويًّا. وطبقة TSX **نظيفة لونيًّا**: لون حرفيّ واحد في كامل اللوحة (`#fff` في `components/page.tsx:321`) — تأكيد أن الانضباط في الـTSX والانحراف في CSS.

**نقطة مضيئة:** `/dashboard/components` **دليل أنماط حيّ حقيقيّ** (17 قسمًا مرقّمًا بفهرس قافز، كل عنصر هو المكوّن التفاعليّ الفعليّ موصولًا بحالة حيّة، يستهلك نفس بيانات الأعضاء التي يُغذّيها للجدول والكرت). أصل ممتاز للمراجعة يُصان ويُطوَّر.

---

## 9) المعمار — «انفصام» يجب أن تعرفه

- **الحزمة لا تملك مظهرها:** `@adeeb/design-system` تشحن `tokens.css` + `fonts.css` **فقط**؛ **100% من CSS المكوّنات في `globals.css` بالتطبيق**. فمثلًا `Button.tsx` يُصدّر `class="abtn"` لكن قواعد `.abtn` معرّفة في `globals.css:848` — والتعليق في المكوّن يقرّ بذلك صراحةً.
- **سطح تصدير منقسم:** `index.ts` يصدّر ~18 بدائيّة (تسويقية)، بينما عُدّة اللوحة الأنضج (`DataTable`/`Modal`/`Toast`/`Tabs`/`Pagination`/`DropdownMenu`) تعيش في `dashboard/_components` و**لا تُصدَّر**.
- **تكرار مفاهيم:** ست عائلات بطاقات (`.acard/.mc/.pv-card/.tm-card/.bc-card/.act-card`)، وأربعة تطبيقات اختيار/قوائم، ونمط segmented في مكانين (`.tabs-seg` و`.mdl-seg`).
- **إعادة بناء بدائيّات محليًّا بدل إعادة الاستخدام (من TSX):**
  - `DataTable.tsx:47-61` يبني خانة اختيار متحرّكة من `<input>` + SVG خام (‏`TableCheck`‏) بدل استيراد `Checkbox` من الحزمة.
  - منتقي الحالة في نموذج العضو أزرار `.mdl-seg` خام (`MembersView.tsx:438`) يكرّر `<Tabs variant="segmented">` الموجود.
  - `Toolbar.tsx:11-41` (`FilterSelect`) يدويّ الصنع (مستمعات إغلاق/Escape) بدل `DropdownMenu` (Radix) أو `Select`.
  - خرائط الحالة→النغمة مكرّرة في مكانين (`components/page.tsx:44` و`MembersView.tsx:37`)، ومصفوفة الأشهر مكرّرة (`DashboardShell.tsx:11` و`data.ts:42`).
  - أزرار الإجراءات الجماعية ماركب `<button className="tb-ba">` خام بدل `<Button>`.
- **تلوّث القالب:** `page.module.css` (بقايا Next) يعرّف لوحة أجنبية (`#fafafa/#000/#666`) وخطّ `--font-geist-sans` خارج `tokens.css` — يتيم لكنه فخّ. يُحذف.
- **محتوى نائب (placeholder):** المستخدم مثبَّت في الكود `USER = {name:"محمد"…}` (`DashboardShell.tsx:14`)، وصفحة «نظرة عامة» ثلاث بطاقات إحصاء ثابتة، وشعار الشريط حرف «أ» ثابت — يُربَط بالمصادقة والبيانات لاحقًا.

**الأثر:** «نظام التصميم» غير قابل للنقل (يكسر هدف إعادة استخدامه في الجوال)، ولا مكان واحد يقول «هكذا تبدو أديب».

### 9‑ب) تناقض واجهة البرمجة والتسمية (اتّساق «العقد» لا المظهر) ⚠️
الهوية البصرية متماسكة، لكن **عقد المكوّنات (props/التسمية) غير موحّد** — وهذا يُربك التطوير ويُنتج انحرافًا مستقبليًّا:

| التناقض | التفصيل | الدليل |
|---------|---------|--------|
| `variant` مقابل `tone` | اللون الدلاليّ **نمطٌ** في الزرّ (`variant="danger"`) لكنه **نغمةٌ** في الشارة/التنبيه (`tone="danger"` + `variant="soft/solid…"`) | `components/page.tsx:124-147` مقابل `173-192` |
| `error` مقابل `danger` | المعنى المدمّر اسمه `error` في التوست، و`danger` في كل مكان آخر، و`error` (ثالثًا) لنصّ تحقّق الحقل | `ToastProvider.tsx:7` · `Field` · `Badge/Alert` |
| لا تعداد نغمات واحد | الشارة/التنبيه = `neutral·info·success·warning·danger` ؛ التوست = `success·error·info·warning` ؛ خرائط الأعضاء = `success·warning·danger·neutral` | ثلاث مجموعات مختلفة |
| `.dg` (صنف) مقابل `danger` (prop) | الخطر أحيانًا صنف CSS (`tb-ba dg`, `mc-role-danger`) وأحيانًا prop منطقيّ (`DropdownMenu`) وأحيانًا لاحقة نمط (`Button`) | ثلاث كتابات |
| تسمية المعالِجات | `onValueChange` (Tabs/Select) مقابل `onChange` (Radio) مقابل `onSearch/onFilter/onViewChange` (Toolbar) | لكلّ مكوّن اصطلاحه |
| سلالم الأحجام | Avatar = `xs·sm·md·lg·xl` بينما البقية `sm·md·lg` | `Avatar.tsx:4` |

**التوصية:** توحيد العقد: تعداد نغمات واحد (`neutral/info/success/warning/danger` — إلغاء `error`)، وفصل «النغمة» عن «النمط» في كل المكوّنات الدلالية، وتوحيد تسمية المعالِجات والأحجام.

---

## 10) ملخّص المراجعة

### نقاط القوة (تُصان ولا تُمسّ)
1. `tokens.css` منضبط: مقاييس كاملة، `:root` محصور، تدرّجات هوية مركزية، ثيم فاتح/داكن/يدوي في مكان واحد.
2. **لغة سطح Aurora** حسٌّ مميّز وناجح — الأساس البصريّ لشخصية اللوحة.
3. **حركة غنيّة موسومة** مع احترام `reduced-motion` في كل مكان.
4. سلوك المكوّنات على Radix/TanStack (فخّ تركيز/ARIA/بوّابة) — «اصنع المظهر وفوّض السلوك».
5. الهوية دائمة الظهور، والتدرّج لا اللون الصلب، وحيلة أرقام Eras — كلّها قواعدك محقَّقة.
6. الأساس التقني حديث: `color-mix(oklab)`، ربط Tailwind بالرموز عبر `@theme inline`، بادئات أصناف متّسقة، `rtl/lang` سليم.
7. **دليل أنماط حيّ** (`/dashboard/components`) بمكوّنات تفاعلية حقيقية — أصل ممتاز للمراجعة والحوكمة. وطبقة TSX نظيفة لونيًّا (لون حرفيّ واحد فقط).

### مصفوفة الانحراف (مرتّبة بالأثر)
| # | الانحراف | الأثر | الإصلاح |
|---|----------|-------|---------|
| 1 | لون دلاليّ واحد بـ2–3 قيَم (جيلان متعايشان) | لا «أحمر/أخضر أديب» واحد | حسم الأنصع + تحديث الرموز + حذف المكرّرات |
| 2 | Aurora منسوخ لا مُرمَّز (قاعدة/نسبة متذبذبة) | لا يتطابق سطحان تمامًا | `--surface/-border/-shadow-aurora` واحد |
| 3 | انفصام معماري (الحزمة بلا CSS، تصدير منقسم) | نظام غير قابل للنقل | نقل CSS المكوّنات للحزمة + سطح تصدير واحد |
| 4 | مئات القيم اللونية الحرفية (تُعيد كتابة رموز) | الهوية غير قابلة للضبط مركزيًّا | تحويل hex الهوية إلى رموز + `stylelint` |
| 5 | سلّم الاستدارة شبه ميّت (16/18 خارج المقياس) | تفاوت خفيّ يُقرأ «غير مصقول» | توسيع السلّم + استبدال الأرقام |
| 6 | لا مقياس on-dark ولا scrim مُرمَّز | ~18 درجة يتيمة | مقياسا `--on-dark-*` و`--scrim` |
| 7 | البكسل الحرفي للطباعة/الأوزان بدل السلالم | لا نظام طباعة مُنفَّذ | سلّم طباعة مُسمّى مُستهلَك |
| 8 | الشعار الرسمي غير مستخدَم في اللوحة | فجوة هوية في أبرز نقطة | إدراج الشعار (موسّع) + مونوغرام (ريّل) |
| 9 | ظلّ ملوّن غير مُرمَّز + مدد حركة حرفية | تذبذب طفيف | `--shadow-brand-*` + توحيد المدد |
| 10 | بقايا (Geist، تعليقات إعادة التصريف، تكرار البطاقات) | ضوضاء وفخاخ | تنظيف |
| 11 | تناقض عقد المكوّنات (`variant`/`tone`، `error`/`danger`، لا تعداد نغمات واحد) | إرباك تطويريّ + انحراف مستقبليّ | توحيد العقد (تعداد + فصل نغمة/نمط) |
| 12 | إعادة بناء بدائيّات محليًّا (خانة اختيار/segmented/مرشّح) بدل إعادة الاستخدام | تكرار وتباعد | استيراد بدائيّات الحزمة |

### خريطة الترحيل المقترحة (بلا كسر بصريّ)
1. **توحيد الدلاليّات** — حسم الأنصع، تحديث `--success/-warning/-danger`، حذف `#1f8a5b`/`#c0392b`.
2. **تقنين Aurora** — رمز واحد يحلّ محلّ ~80 نسخة.
3. **مقاييس ناقصة** — `--on-dark-*`، `--scrim`، `--shadow-brand-*`.
4. **hex الهوية → رموز** — استبدال `#274060`/`#335c81` الحرفيَّين بالرموز.
5. **الاستدارة** — توسيع السلّم واستبداله.
6. **الطباعة** — سلّم مُسمّى.
7. **الشعار في الهيكل** — إدراج الرسمي.
8. **المعمار والعقد** — نقل CSS المكوّنات للحزمة، توحيد التصدير، توحيد عقد الـprops (تعداد نغمات واحد، فصل نغمة/نمط، تسمية موحّدة)، إلغاء البدائيّات المكرّرة، حذف البقايا.
9. **الفرض** — `stylelint` يمنع hex/rgba/الاستدارة الحرفية في CSS التطبيق، وقائمة مراجعة PR — ليصير `tokens.css` مصدرًا وحيدًا **بالفعل لا بالاسم**.

---

> **التقييم الختاميّ:** هوية ناضجة الحسّ، عالية الجودة في التفاصيل، لكنها **مكتوبة مرّتين ومنسوخة عشرات المرّات**. العمل القادم ليس تصميمًا بل **هندسةً**: نرفع ما في رأس المطوّر ووصفاته المتناثرة إلى رموز مُسمّاة مفروضة — فتُصبح اللوحة تبدو كما تبدو الآن تمامًا، لكن بمصدر حقيقة واحد قابل للضبط والنقل.
