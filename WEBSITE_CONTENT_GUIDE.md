# دليل جداول محتوى الموقع الشامل - نادي أدِيب

## 📋 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [تفاصيل الجداول](#تفاصيل-الجداول)
3. [التحديثات الأخيرة](#التحديثات-الأخيرة)
4. [أمثلة الاستخدام](#أمثلة-الاستخدام)
5. [التكامل مع لوحة التحكم](#التكامل-مع-لوحة-التحكم)

---

## 🎯 نظرة عامة

تم إنشاء 4 جداول جديدة لإدارة محتوى الموقع الإلكتروني لنادي أدِيب:

1. **أعمالنا (works)** - عرض أعمال ومشاريع النادي
2. **الإنجازات (achievements)** - توثيق إنجازات النادي
3. **الشركاء (sponsors)** - إدارة الشركاء والرعاة
4. **الأسئلة الشائعة (faq)** - الإجابة على الأسئلة المتكررة

**تاريخ الإنشاء:** 2026-01-17  
**آخر تحديث:** 2026-01-18

---

## 📊 تفاصيل الجداول

### 1️⃣ جدول الإنجازات (achievements)

**الوصف:** يوثق إنجازات النادي بتنسيق رقمي مع أيقونات

#### الحقول:

| الحقل | النوع | الوصف | مطلوب |
|-------|------|-------|-------|
| `id` | SERIAL | المعرف الفريد | ✅ |
| `label` | TEXT | التسمية (مثل: عدد الأعضاء) | ✅ |
| `icon_class` | TEXT | فئة الأيقونة Font Awesome | ❌ |
| `count_number` | INTEGER | الرقم المعروض | ✅ |
| `order` | INTEGER | ترتيب العرض | ✅ |
| `plus_flag` | BOOLEAN | إضافة علامة + بعد الرقم | ❌ |
| `created_by` | UUID | منشئ السجل | ❌ |
| `created_at` | TIMESTAMPTZ | تاريخ الإنشاء | تلقائي |

#### الفهارس:
- `idx_achievements_order` - للترتيب
- `idx_achievements_created_by` - للبحث حسب المنشئ

#### سياسات الأمان (RLS):
- **القراءة العامة:** الجميع يمكنهم قراءة جميع الإنجازات
- **الإضافة/التعديل/الحذف:** المستوى 7+ فقط

#### مثال:
```json
{
  "label": "عدد الأعضاء",
  "icon_class": "fa-solid fa-users",
  "count_number": 150,
  "order": 1,
  "plus_flag": true
}
```

---

### 2️⃣ جدول الأعمال (works)

**الوصف:** يحتوي على أعمال ومشاريع النادي المنشورة

#### الحقول:

| الحقل | النوع | الوصف | مطلوب |
|-------|------|-------|-------|
| `id` | SERIAL | المعرف الفريد | ✅ |
| `title` | TEXT | عنوان العمل | ✅ |
| `description` | TEXT | وصف العمل | ❌ |
| `category` | TEXT | الفئة (كتاب، مقال، فيديو) | ❌ |
| `image_url` | TEXT | رابط صورة العمل | ✅ |
| `link_url` | TEXT | رابط العمل | ❌ |
| `order` | INTEGER | ترتيب العرض | ✅ |
| `created_by` | UUID | منشئ السجل | ❌ |
| `created_at` | TIMESTAMPTZ | تاريخ الإنشاء | تلقائي |

#### الفهارس:
- `idx_works_category` - للبحث حسب التصنيف
- `idx_works_order` - للترتيب
- `idx_works_created_by` - للبحث حسب المنشئ

#### سياسات الأمان (RLS):
- **القراءة العامة:** الجميع يمكنهم قراءة جميع الأعمال
- **القراءة الإدارية:** المستوى 7+ يمكنهم رؤية جميع الأعمال
- **الإضافة/التعديل/الحذف:** المستوى 7+ فقط

#### مثال:
```json
{
  "title": "كتاب الأدب العربي",
  "category": "كتاب",
  "image_url": "https://example.com/book.jpg",
  "link_url": "https://example.com/book",
  "order": 1
}
```

---

### 3️⃣ جدول الشركاء (sponsors)

**الوصف:** يدير معلومات الشركاء والرعاة

#### الحقول:

| الحقل | النوع | الوصف | مطلوب |
|-------|------|-------|-------|
| `id` | SERIAL | المعرف الفريد | ✅ |
| `name` | TEXT | اسم الشريك | ✅ |
| `description` | TEXT | وصف الشريك | ❌ |
| `logo_url` | TEXT | رابط الشعار | ✅ |
| `link_url` | TEXT | رابط موقع الشريك | ❌ |
| `badge` | TEXT | شارة الشريك (ذهبي، فضي، برونزي) | ❌ |
| `order` | INTEGER | ترتيب العرض | ✅ |
| `created_by` | UUID | منشئ السجل | ❌ |
| `created_at` | TIMESTAMPTZ | تاريخ الإنشاء | تلقائي |

#### الفهارس:
- `idx_sponsors_order` - للترتيب
- `idx_sponsors_created_by` - للبحث حسب المنشئ

#### سياسات الأمان (RLS):
- **القراءة العامة:** الجميع يمكنهم قراءة جميع الشركاء
- **القراءة الإدارية:** المستوى 7+ يمكنهم رؤية جميع الشركاء
- **الإضافة/التعديل/الحذف:** المستوى 7+ فقط

#### مثال:
```json
{
  "name": "شركة ABC",
  "badge": "شريك ذهبي",
  "logo_url": "https://example.com/logo.png",
  "link_url": "https://example.com",
  "description": "شريك استراتيجي",
  "order": 1
}
```

---

### 4️⃣ جدول الأسئلة الشائعة (faq)

**الوصف:** يحتوي على الأسئلة المتكررة وإجاباتها

#### الحقول:

| الحقل | النوع | الوصف | مطلوب |
|-------|------|-------|-------|
| `id` | SERIAL | المعرف الفريد | ✅ |
| `question` | TEXT | السؤال | ✅ |
| `answer` | TEXT | الإجابة | ✅ |
| `order` | INTEGER | ترتيب العرض | ✅ |
| `created_by` | UUID | منشئ السجل | ❌ |
| `created_at` | TIMESTAMPTZ | تاريخ الإنشاء | تلقائي |
| `updated_at` | TIMESTAMPTZ | تاريخ التحديث | تلقائي |

#### الفهارس:
- `idx_faq_order` - للترتيب
- `idx_faq_created_by` - للبحث حسب المنشئ

#### سياسات الأمان (RLS):
- **القراءة العامة:** الجميع يمكنهم قراءة جميع الأسئلة
- **القراءة الإدارية:** المستوى 7+ يمكنهم رؤية جميع الأسئلة
- **الإضافة/التعديل/الحذف:** المستوى 7+ فقط

#### مثال:
```json
{
  "question": "ما هو نادي أدِيب؟",
  "answer": "نادي أدِيب هو...",
  "order": 1
}
```

---

## 🔄 التحديثات الأخيرة

### Migration 012 (2026-01-18)

تم تحديث بنية الجداول لتتماشى مع التصميم الأصلي:

#### جدول الإنجازات:
- ✅ إضافة: `label`, `icon_class`, `count_number`, `plus_flag`
- ❌ حذف: `title`, `description`, `category`, `image_url`, `achievement_date`, `updated_at`

#### جدول الأعمال:
- ✅ تغيير: `display_order` → `order`
- ❌ حذف: `is_published`, `publish_date`

#### جدول الشركاء:
- ✅ تغيير: `display_order` → `order`
- ❌ حذف: `is_active`, `logo`, `link`

#### جدول الأسئلة الشائعة:
- ✅ تغيير: `display_order` → `order`
- ✅ إضافة: `updated_at` مع trigger تلقائي
- ❌ حذف: `category`, `is_active`

---

## 📝 أمثلة الاستخدام

### إضافة إنجاز:
```sql
INSERT INTO public.achievements (label, icon_class, count_number, plus_flag, "order", created_by)
VALUES (
    'عدد الأعضاء',
    'fa-solid fa-users',
    150,
    true,
    1,
    auth.uid()
);
```

### إضافة عمل:
```sql
INSERT INTO public.works (title, category, image_url, link_url, "order", created_by)
VALUES (
    'مسابقة الشعر السنوية',
    'أدبي',
    'https://example.com/image.jpg',
    'https://example.com/event',
    1,
    auth.uid()
);
```

### إضافة شريك:
```sql
INSERT INTO public.sponsors (name, badge, logo_url, link_url, description, "order", created_by)
VALUES (
    'شركة ABC',
    'ذهبي',
    'https://example.com/logo.png',
    'https://abc.com',
    'شريك استراتيجي',
    1,
    auth.uid()
);
```

### إضافة سؤال شائع:
```sql
INSERT INTO public.faq (question, answer, "order", created_by)
VALUES (
    'كيف يمكنني الانضمام للنادي؟',
    'يمكنك التسجيل عبر الموقع الإلكتروني',
    1,
    auth.uid()
);
```

---

## 💻 التكامل مع لوحة التحكم

### دوال JavaScript

#### تحميل الإنجازات:
```javascript
async function loadWebsiteAchievements() {
    const { data: achievements, error } = await sb
        .from('achievements')
        .select('*')
        .order('order', { ascending: true });
    
    achievements.forEach(ach => {
        console.log(`${ach.label}: ${ach.count_number}${ach.plus_flag ? '+' : ''}`);
    });
}
```

#### حفظ إنجاز:
```javascript
async function saveAchievement(data) {
    const { data: result, error } = await sb
        .from('achievements')
        .insert([{
            label: data.label,
            count_number: data.count,
            icon_class: data.icon,
            plus_flag: data.plusFlag,
            order: data.order,
            created_by: currentUser.id
        }]);
}
```

#### تحميل الأعمال:
```javascript
async function loadWebsiteWorks() {
    const { data: works, error } = await sb
        .from('works')
        .select('*')
        .order('order', { ascending: true });
}
```

### نماذج HTML

#### نموذج إضافة إنجاز:
```html
<form id="achievementForm">
    <input type="text" id="achievementLabel" placeholder="عدد الأعضاء" required />
    <input type="number" id="achievementCount" min="0" required />
    <input type="text" id="achievementIcon" placeholder="fa-solid fa-trophy" required />
    <input type="checkbox" id="achievementPlus" checked />
    <label>إضافة علامة +</label>
    <input type="number" id="achievementOrder" min="0" />
</form>
```

---

## 🔐 ملخص سياسات الأمان

| الجدول | القراءة العامة | الإضافة | التعديل | الحذف |
|--------|----------------|---------|---------|--------|
| **achievements** | الكل | 7+ | 7+ | 7+ |
| **works** | الكل | 7+ | 7+ | 7+ |
| **sponsors** | الكل | 7+ | 7+ | 7+ |
| **faq** | الكل | 7+ | 7+ | 7+ |

**المستوى 7+** يشمل:
- عضو إداري (المستوى 7)
- قائد لجنة HR/QA (المستوى 8)
- رئيس مجلس الإدارة (المستوى 9)
- رئيس النادي (المستوى 10)

---

## 🔄 كيفية تطبيق التحديثات

### الخطوة 1: تشغيل Migrations
```bash
# تطبيق Migrations بالترتيب
psql -f supabase/migrations/010_create_website_content_tables.sql
psql -f supabase/migrations/011_website_content_rls_policies.sql
psql -f supabase/migrations/012_update_website_content_structure.sql
```

### الخطوة 2: التحقق
```sql
-- التحقق من الجداول
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('works', 'achievements', 'sponsors', 'faq');

-- التحقق من الأعمدة
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'achievements';
```

---

## 📊 الإحصائيات

- **عدد الجداول:** 4
- **إجمالي الحقول:** 36 حقل
- **عدد الفهارس:** 12 فهرس
- **عدد Triggers:** 1 trigger (faq.updated_at)
- **عدد RLS Policies:** 16 سياسة

---

## ⚠️ ملاحظات مهمة

1. **الترتيب:** جميع الجداول تستخدم `order` للتحكم في ترتيب العرض
2. **created_by:** تمت إضافة هذا الحقل لجميع الجداول لتتبع من أنشأ السجل
3. **الإنجازات:** البنية الجديدة تدعم الأيقونات وعلامة + بشكل مباشر
4. **التوافق:** التصميم متوافق 100% مع الموقع الأصلي index.html

---

## ✨ الخلاصة

تم إنشاء وتحديث 4 جداول لإدارة محتوى الموقع بنجاح:
- ✅ **أعمالنا** - 9 حقول
- ✅ **الإنجازات** - 8 حقول
- ✅ **الشركاء** - 9 حقول
- ✅ **الأسئلة الشائعة** - 7 حقول

النظام الآن جاهز للاستخدام! 🎉

---

**تاريخ الإنشاء:** 2026-01-17  
**آخر تحديث:** 2026-01-18  
**الإصدار:** 2.0
