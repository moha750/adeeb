/**
 * مقاساتُ الهيكل التي يحتاجها أكثرُ من مكان.
 * الشريطُ المصغّر يجلس فوق شريط التبويبات، فالرقمُ نفسُه يُقرأ في الاثنين.
 */

/** ارتفاعُ شريط التبويبات فوق الحافّة الآمنة. */
export const TAB_BAR_H = 64;

/** ارتفاعُ المشغّل المصغّر. */
export const MINI_H = 62;

/** أدنى مساحةِ لمسٍ يقبلها القانون: ٤٤ نقطة. */
export const TOUCH = 44;
