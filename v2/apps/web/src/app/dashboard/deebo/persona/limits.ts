/**
 * حدودُ الطبع — **ملفٌّ بلا إطار** يقرؤه الخادمُ والعميل معًا (سُنّةُ `vocab.ts`).
 *
 * ولا تعيش هذه في `actions.ts`: ملفُّ `"use server"` لا يُصدِّر إلّا دوالَّ غيرَ متزامنة،
 * فثابتٌ يُصدَّر منه يُسقط البناء. ولا في `data.ts`: ذاك `server-only` فلا يبلغ الشاشة.
 */

/**
 * حقولُ النصّ التي تدخل التوجيه (الأسئلةُ المقترَحة زينةُ شاشةٍ فلا تُحسب).
 * والحدودُ ليست منها: صارت أحكامًا مفردةً تُحسب على حدة (م١٠).
 */
export const PROMPT_FIELDS = ["identity", "tone", "prohibitions", "unknownAnswer"] as const;

/**
 * ميزانُ الطبع بالحروف — مجموعُ ما يدخل التوجيه.
 *
 * ورقمُه من قياسٍ لا من ذوق: طبعُه اليومَ نحو خمسة آلافٍ وخمسمائة حرف، فالميزانُ يترك
 * فسحةً للنموّ ويمنع التضاعف. **وعلّةُ السقف** أنّ النموذج يوزّع انتباهَه على ما يُعطى:
 * توجيهٌ متورّمٌ يُنسي أوّلَه، فيصير كلُّ سطرٍ يُضاف إضعافًا لما قبله لا زيادةَ إحكام.
 */
export const PROMPT_BUDGET = 9000;
