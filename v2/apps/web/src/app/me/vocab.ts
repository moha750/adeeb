/**
 * مفرداتُ بيت الحساب — **بلا إطارٍ ولا `server-only`**، فيقرؤها الخادمُ والعميلُ معًا.
 *
 * وموضعُها ههنا لا في `actions.ts`: ذاك ملفُّ `"use server"` ولا يُصدِّر إلّا دوالَّ
 * لا متزامنة، فثابتٌ فيه يُسقط البناء.
 */

/**
 * حدُّ سبب الخروج من العضويّة — توأمُه في القاعدة `length(btrim(reason)) >= 5` على
 * `membership_exit_requests`، وهو حدُّ سبب الإنذار في هذا المستودع نفسُه.
 *
 * والسببُ **إجباريٌّ** بأمر المالك (٢٠٢٦-٠٨-٢٠) في البابين معًا: زرِّ الإنهاء وطلبِه.
 * فخروجُ إنسانٍ من النادي واقعةٌ تُقرأ بعد سنة، ولا تُقرأ بلا علّتها.
 */
export const EXIT_REASON_MIN = 5;
