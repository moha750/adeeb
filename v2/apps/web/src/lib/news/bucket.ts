/**
 * اسم دلو وسائط الأخبار — **المصدر الواحد**، ويُقرأ من الطرفين.
 *
 * مفصولٌ عن `media.ts` لأنّ ذاك `server-only` (يمسّ الحذف والمسارات)، والمتصفّح
 * يحتاج الاسم وحده ليرفع إلى الرابط الموقّع. فلا يُكتب الاسم مرّتين ولا ينحرف.
 */
export const NEWS_BUCKET = "images";
