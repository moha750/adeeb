// وزنُ أيقونات أديب — **المصدر الواحد**. غيّره هنا فيتبعه الموقعُ كلّه.
//
// يُقرأ من موضعين لا ثالث لهما:
//  1. `IconDefaults` (سياق Phosphor في جذر التخطيط) — يخدم كلّ أيقونةٍ في مكوّنٍ عميليّ.
//  2. مداخل `@phosphor-icons/react/dist/ssr` — مكوّناتٌ خادميّة **لا تقرأ سياقًا بطبعها**
//     (`SSRBase` يقع على `regular` افتراضًا)، فيُمرَّر لها الرمز صراحةً. تُستعمل حيث لا
//     يجوز مدخلُ العميل: مكوّنٌ خادميّ لا يحتمل `createContext`.
export const ICON_WEIGHT = "duotone" as const;

// **وزنُ المستثنَين — واحدٌ لهم جميعًا.** أسماؤهم في `app/_components/glyphs.tsx`، وما خرج
// من duotone خرج إلى `bold` بلا قسمة: رموزُ الفعل والأسهمُ والأدواتُ والشعاراتُ والحالاتُ
// الدائريّة ونجمةُ التمييز سواء. (كانت قسمةً بوزنين — `fill` للشعارات والحالات — فأُلغيت
// بقرار المالك ٢٠٢٦-٠٨-٠٥: المستثنى وزنٌ واحد يُعرَف به.)
export const ICON_WEIGHT_EXCEPTION = "bold" as const;
