# قائمة موت V1 — كلّ دَينٍ مؤجَّل في مكان واحد

> **لماذا هذا الملفّ:** استراتيجيّتنا كلّها «أضِف ولا تحذف، وأسقِط حين يموت V1».
> وهي سليمة — بشرطٍ واحد: **أن يموت V1 فعلًا**. وإلّا صار الدَّين أبديًّا والوصخ يتراكم
> في ذاكرةٍ تُنسى ووثائق تتبعثر.
>
> فهذا الملفّ يحوّل الخوف إلى إجراء: **يوم يموت V1، نفّذ ما هنا في جلسة واحدة.**
> ولا يُضاف دَينٌ مؤجَّل إلى القاعدة إلّا ويُسجَّل هنا في نفس الترحيل.

## متى يموت V1؟

**ليس بحذف ملفّاته** — حُذفت من الفرع في `e0b16a6` وadeeb.club ما زال يعمل، لأنّ النشر من `main`.

**يموت حين يحلّ V2 محلّه على adeeb.club.** وذلك يتطلّب — ولم يبدأ بعد:

- V2 **لا يعمل خارج `localhost:3000`**: لا `output: 'export'` في `next.config.ts`، وفيه ٣ ملفّات `"use server"` تمنع التصدير الساكن أصلًا.
- GitHub Pages **لا يخدم إلّا الساكن** → V2 يحتاج استضافة Node (Vercel/Netlify/غيرها).
- لا `.github/workflows`، ولا `vercel.json`، ولا أيّ إعداد نشر.
- وV2 فيه **٣ بنود** مقابل ٦٦ قسمًا في V1.

> **فالطريق إلى موت V1 ليس حذفًا ولا دفعًا — بل جعل V2 قابلًا للنشر.** كلّ بندٍ أدناه رهينةُ ذلك.

---

## البنود المؤجَّلة

### ١) `user_roles.role_id` + `role_permissions.role_id`

**الشكل:** مفتاح مزدوج (رقم + اسم) يزامنه تريغر `sync_role_key`.
**يمنعه:** `admin/dashboard.js` الحيّ يقرأ `role_id` في **٨٢ موضعًا**.

```sql
alter table user_roles       drop column role_id;
alter table role_permissions drop column role_id;
```

### ٢) تريغرا `sync_role_key`

**الشكل:** تريغران يزامنان المفتاحين — لا لزوم لهما بعد إسقاط `role_id`.
**يمنعه:** البند ١.

```sql
drop trigger if exists user_roles_sync_role_key on user_roles;
drop trigger if exists role_permissions_sync_role_key on role_permissions;
drop function if exists public.sync_role_key();
```

### ٣) `assign_position(p_role integer)`

**الشكل:** معاملان لنفس المعنى (`p_role` و`p_role_name`).
**يمنعه:** لا شيء تقنيًّا — V2 يمرّر الاسم. لكن أسقِطه مع البند ١ لتبقى الدالّة متّسقة.
**تحذير:** الإسقاط **صريح** ثمّ إعادة إنشاء — لا `create or replace`، وإلّا صار حِملًا زائدًا يلتبس فيه النداء بسبعة معاملات.

### ٤) `roles.role_category`

**الشكل:** عمود `NOT NULL` **ميّت** — صفر دالّة تقرؤه، صفر مقارنة بقيمته في أيّ كود. ومع ذلك يعترض كلّ منصب جديد، ويناقض `council_type` (رئيس التنفيذيّ مصنَّف `administrative_council`؛ منسّق القسم مصنَّف `committee`).
**يمنعه:** موضعان في V1 **يطلبانه صراحةً ثمّ يهملان قيمته**:
- `admin/js/results-analytics.js:1453`
- `admin/js/surveys-manager.js:1698`

> **ملاحظة:** حذفُ V1 من الفرع جعل إصلاحهما **أصعب** — السطران يعيشان على `main` وحده.
> إن أردت إسقاطه قبل موت V1: احذف `role_category` من سلسلتَي `select` هناك، وانشر، ثمّ أسقِط.

```sql
alter table roles drop column role_category;
```

### ٥) `committees.department_id = NULL` للإدارتين (22، 23)

**الشكل:** فراغٌ متروك عمدًا. لم يعد يحمل معنى — `council_id` يقول التبعيّة صراحةً.
**يمنعه:** V1 يقسّم اللجان بـ`.is('department_id', null)` في **٦ مواضع** في `dashboard.js`.
**بعد موته:** الفراغ يبقى فراغًا (لا قيمة تُوضع فيه) — لكن يُزال أي قارئ يستنتج منه.

### ٦) `activity_coordinator`

**الشكل:** دور مهجور — **صفر شاغل**، ولم يُذكر في هيكلة النادي التي أقرّها المالك.
**يمنعه:** ٣ دوالّ حيّة (`list`/`assign`/`revoke`) + دوالّ الحضور + ٥ ملفّات في V1.
**بعد موته:** أزِل واجهته ودوالّه **معًا**، ثمّ الصفّ.

---

## سُدّد بالفعل

| البند | متى | كيف |
|---|---|---|
| `election_vote_weights` | 2026-07-15 | أُسقط — صفر قارئ. الوزن صار `roles.vote_weight` NOT NULL بلا DEFAULT، فالنسيان مستحيل بنيويًّا. البصمة قبل/بعد متطابقة (180 عضوًا، 199.5). |
| المفتاح المركّب `committees_department_council_fkey` | 2026-07-15 | أُسقط واستُبدل بتريغر — كان يكسر `dashboard.js:3583` حيًّا (PGRST201). |
| مفتاحا `committees → roles` | 2026-07-15 | أُسقطا واستُبدلا بتريغر — قنبلة PostgREST نائمة. |
| `role_id` في دوالّ الحافّة | 2026-07-15 | `create-member-directly` و`migrate-accepted-member` صارا بالاسم (مصدرًا). |

---

## قاعدة دائمة — لا تُنسَ

**المفتاح الأجنبيّ ليس إضافيًّا من منظور PostgREST.** يستنتج التضمين من المفاتيح، فالمفتاح **الثاني** إلى جدولٍ مُضمَّن يُبطِل **كلّ** تضمينٍ قائم — `PGRST201`. لا يكسره تغييرُ عمود، بل **وجودُ خيارَين**.

> كسر هذا `dashboard.js:3583` ساعاتٍ في 2026-07-15، من مفتاحٍ ظننتُه أنيقًا.

**قبل أيّ FK ثانٍ إلى جدولٍ مُضمَّن:** `grep "select('\*, X("` في كلّ قارئ حيّ. وإن وُجد — استعمل تريغرًا لا مفتاحًا.
