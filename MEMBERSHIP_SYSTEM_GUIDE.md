# دليل نظام إدارة العضوية المحدّث

## 📋 نظرة عامة

تم تحديث نظام إدارة العضوية ليشمل **3 أقسام رئيسية** مع **أرشفة تلقائية** لفترات فتح التسجيل.

---

## 🎯 الأقسام الجديدة

### 1️⃣ **طلبات العضوية** (عرض فقط)
- **الصلاحية المطلوبة:** مستوى 5+
- **الوظيفة:** عرض جميع الطلبات بدون إمكانية التعديل
- **المميزات:**
  - عرض الطلبات في جدول
  - إحصائيات شاملة (جديدة، قيد المراجعة، مقبولة، مرفوضة)
  - البحث والفلترة حسب الحالة واللجنة
  - تصدير البيانات إلى CSV
- **الهدف:** السماح للمستويات الأدنى بالاطلاع على الطلبات دون إجراء تعديلات

### 2️⃣ **مراجعة الطلبات** (مع الإجراءات)
- **الصلاحية المطلوبة:** مستوى 8+
- **الوظيفة:** مراجعة الطلبات واتخاذ القرارات
- **المميزات:**
  - جميع مميزات قسم العرض
  - زر "عرض" لكل طلب لفتح نافذة التفاصيل
  - قبول/رفض الطلبات
  - إضافة ملاحظات إدارية
  - تغيير حالة الطلب
  - حذف الطلبات
- **الهدف:** إدارة كاملة للطلبات من قبل الموارد البشرية

### 3️⃣ **أرشيف فتح التسجيل**
- **الصلاحية المطلوبة:** مستوى 7+
- **الوظيفة:** عرض سجل تاريخي لفترات التسجيل
- **المميزات:**
  - عرض جميع الأرشيفات السابقة
  - إحصائيات شاملة لكل فترة:
    - تاريخ الفتح والإغلاق
    - عدد الطلبات الإجمالي
    - توزيع الطلبات حسب الحالة
    - معدل القبول والرفض
    - توزيع الطلبات حسب اللجان
    - متوسط الطلبات اليومي
  - عرض تفاصيل كل أرشيف
  - تحميل الأرشيف كملف JSON
  - إنشاء أرشيف يدوي
- **الهدف:** الاحتفاظ بسجل تاريخي كامل لجميع فترات التسجيل

---

## 🔄 الأرشفة التلقائية

### كيف تعمل؟

1. **عند إغلاق باب التسجيل:**
   - يظهر للمستخدم خيار إنشاء أرشيف تلقائي
   - يمكن اختيار "نعم، إنشاء أرشيف" أو "لا، إغلاق فقط"

2. **ما يتم أرشفته:**
   - تاريخ فتح وإغلاق التسجيل
   - إجمالي الطلبات وتوزيعها حسب الحالة
   - توزيع الطلبات حسب اللجان
   - معدلات القبول والرفض
   - متوسط الطلبات اليومي
   - نسخة من إعدادات التسجيل

3. **الفترة المؤرشفة:**
   - من تاريخ إغلاق آخر أرشيف (أو تاريخ أول طلب)
   - حتى تاريخ الإغلاق الحالي

### إنشاء أرشيف يدوي

يمكن للمستخدمين ذوي الصلاحية (مستوى 7+) إنشاء أرشيف يدوي في أي وقت من قسم "أرشيف التسجيل".

---

## 🗄️ قاعدة البيانات

### جدول جديد: `membership_registration_archives`

```sql
CREATE TABLE membership_registration_archives (
    id UUID PRIMARY KEY,
    archive_name TEXT NOT NULL,
    opened_at TIMESTAMPTZ NOT NULL,
    closed_at TIMESTAMPTZ NOT NULL,
    total_applications INTEGER,
    new_applications INTEGER,
    under_review_applications INTEGER,
    accepted_applications INTEGER,
    rejected_applications INTEGER,
    archived_applications INTEGER,
    committees_data JSONB,
    settings_snapshot JSONB,
    statistics JSONB,
    created_by UUID REFERENCES profiles(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### دالة الأرشفة: `create_membership_archive()`

دالة SQL تقوم بـ:
- حساب جميع الإحصائيات تلقائياً
- جمع بيانات اللجان
- حفظ نسخة من الإعدادات
- إنشاء سجل أرشيف كامل

---

## 🔐 الصلاحيات

| القسم | المستوى المطلوب | الوصف |
|------|-----------------|-------|
| إعدادات التسجيل | 8+ | التحكم في فتح/إغلاق التسجيل |
| اللجان المتاحة | 8+ | إدارة اللجان المتاحة للتسجيل |
| طلبات العضوية (عرض) | 5+ | عرض الطلبات فقط |
| مراجعة الطلبات | 8+ | قبول/رفض الطلبات |
| أرشيف التسجيل | 7+ | عرض وتحميل الأرشيفات |

---

## 📊 الإحصائيات المتوفرة

### في قسم طلبات العضوية (عرض):
- طلبات جديدة
- قيد المراجعة
- مقبولة
- مرفوضة

### في قسم الأرشيف:
- إجمالي الأرشيفات
- إجمالي الطلبات المؤرشفة
- آخر فترة تسجيل
- معدل القبول الإجمالي

### في تفاصيل كل أرشيف:
- الفترة الزمنية والمدة
- توزيع الطلبات حسب الحالة
- معدل القبول والرفض
- متوسط الطلبات اليومي
- توزيع الطلبات حسب اللجان

---

## 🚀 كيفية الاستخدام

### للمستخدمين (مستوى 5+):
1. افتح "إدارة العضوية" من القائمة الجانبية
2. اختر "طلبات العضوية"
3. استعرض الطلبات والإحصائيات
4. استخدم البحث والفلترة للعثور على طلبات محددة
5. صدّر البيانات إلى CSV عند الحاجة

### للموارد البشرية (مستوى 8+):
1. افتح "مراجعة الطلبات"
2. اضغط على "عرض" لأي طلب
3. راجع التفاصيل الكاملة
4. اختر الحالة المناسبة (قبول/رفض)
5. أضف ملاحظات إدارية
6. احفظ التغييرات

### للمديرين (مستوى 7+):
1. افتح "أرشيف التسجيل"
2. استعرض جميع الأرشيفات السابقة
3. اضغط على "عرض التفاصيل" لأي أرشيف
4. حمّل الأرشيف كملف JSON عند الحاجة
5. أنشئ أرشيف يدوي إذا لزم الأمر

### عند إغلاق التسجيل (مستوى 8+):
1. افتح "إعدادات التسجيل"
2. ألغِ تفعيل "فتح باب التسجيل"
3. اضغط "حفظ الإعدادات"
4. اختر "نعم، إنشاء أرشيف" في النافذة المنبثقة
5. سيتم إنشاء الأرشيف تلقائياً

---

## 📁 الملفات المعدّلة

### قاعدة البيانات:
- `supabase/migrations/034_create_membership_registration_archives.sql` (جديد)

### HTML:
- `admin/dashboard.html`
  - تم تقسيم قسم العضوية إلى 3 أقسام منفصلة
  - إضافة قسم الأرشيف

### JavaScript:
- `admin/dashboard.js`
  - تحديث القائمة المنسدلة لدعم الأقسام الجديدة
  - إضافة معالجات للأقسام الثلاثة

- `admin/js/membership-manager.js`
  - إضافة `loadApplicationsView()` - عرض الطلبات فقط
  - إضافة `loadApplicationsReview()` - مراجعة الطلبات
  - إضافة `loadArchives()` - عرض الأرشيفات
  - إضافة `viewArchive()` - عرض تفاصيل أرشيف
  - إضافة `downloadArchive()` - تحميل أرشيف
  - إضافة `createManualArchive()` - إنشاء أرشيف يدوي
  - تحديث `saveMembershipSettingsHandler()` للأرشفة التلقائية

### CSS:
- `admin/dashboard.css`
  - إضافة أنماط القائمة المنسدلة
  - أنماط العناصر الفرعية

---

## ⚠️ ملاحظات مهمة

1. **الأرشفة اختيارية:** عند إغلاق التسجيل، يمكن اختيار عدم إنشاء أرشيف
2. **الأرشيف دائم:** لا يمكن حذف الأرشيفات إلا من قبل المستوى 10 (رئيس النادي)
3. **البيانات محفوظة:** الأرشيف يحتوي على نسخة كاملة من البيانات بصيغة JSON
4. **الصلاحيات محكمة:** كل قسم له صلاحيات مستقلة في قاعدة البيانات
5. **الأداء محسّن:** تم إضافة فهارس لجميع الجداول لتحسين الأداء

---

## 🔧 الصيانة

### تشغيل Migration:
```bash
# في مجلد المشروع
supabase db push
```

### التحقق من الجدول:
```sql
SELECT * FROM membership_registration_archives ORDER BY closed_at DESC;
```

### إنشاء أرشيف يدوياً (SQL):
```sql
SELECT create_membership_archive();
```

---

## 📞 الدعم

في حالة وجود مشاكل:
1. تحقق من صلاحيات المستخدم
2. راجع console logs في المتصفح
3. تأكد من تشغيل migration قاعدة البيانات
4. تحقق من RLS policies في Supabase

---

## ✅ الخلاصة

النظام الجديد يوفر:
- ✅ فصل واضح بين العرض والإجراءات
- ✅ صلاحيات محكمة لكل قسم
- ✅ أرشفة تلقائية ذكية
- ✅ إحصائيات شاملة ومفصلة
- ✅ سجل تاريخي كامل لجميع فترات التسجيل
- ✅ إمكانية تحميل البيانات بصيغة JSON
- ✅ واجهة مستخدم سهلة وواضحة

**تم التطوير بعناية لضمان عدم حدوث أخطاء والحفاظ على سلامة البيانات.** 🎉
