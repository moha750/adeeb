# دليل نظام المقابلات والقبول المتقدم

## 📋 نظرة عامة

تم تطوير نظام متكامل لإدارة عملية القبول بمراحل متعددة، يشمل المقابلات وإدارة الأعضاء المقبولين.

---

## 🎯 مراحل القبول

### **المرحلة 1: مراجعة الطلبات**
- **الصلاحية:** مستوى 8+
- **الخيارات المتاحة:**
  1. ✅ **قبول للمقابلة** → ينتقل الطلب إلى قسم المقابلات
  2. ❌ **رفض** → يتم رفض الطلب نهائياً
  3. 👁️ **مراجعة** → يوضع الطلب قيد المراجعة

### **المرحلة 2: المقابلات**
- **الصلاحية:** مستوى 7+
- **الوظائف:**
  - جدولة مقابلات للمتقدمين المقبولين
  - إدارة تفاصيل المقابلة (التاريخ، النوع، الموقع)
  - تسجيل نتيجة المقابلة (قبول/رفض)
- **الخيارات المتاحة:**
  1. ✅ **قبول** → ينتقل إلى قسم المقبولين + يحصل على رقم عضوية
  2. ❌ **رفض** → يتم رفض المتقدم بعد المقابلة

### **المرحلة 3: المقبولين**
- **الصلاحية:** مستوى 5+ (عرض فقط)
- **المعلومات:**
  - رقم العضوية (يتم توليده تلقائياً)
  - معلومات العضو الكاملة
  - اللجنة المعينة
  - حالة العضوية (نشط/غير نشط/معلق/منتهي)

---

## 🗄️ قاعدة البيانات

### **1. تحديث جدول الطلبات**

```sql
-- حالات جديدة
ALTER TABLE membership_applications 
ADD CONSTRAINT membership_applications_status_check 
CHECK (status IN (
    'new',                      -- جديدة
    'under_review',             -- قيد المراجعة
    'approved_for_interview',   -- مقبول للمقابلة ⭐ جديد
    'rejected',                 -- مرفوضة
    'archived'                  -- مؤرشفة
));

-- حقول جديدة
ALTER TABLE membership_applications 
ADD COLUMN approved_for_interview_at TIMESTAMPTZ,
ADD COLUMN approved_for_interview_by UUID REFERENCES profiles(id);
```

### **2. جدول المقابلات**

```sql
CREATE TABLE membership_interviews (
    id UUID PRIMARY KEY,
    application_id UUID REFERENCES membership_applications(id),
    interview_date TIMESTAMPTZ NOT NULL,
    interview_location TEXT,
    interview_type TEXT, -- in_person, online, phone
    meeting_link TEXT,
    interviewer_id UUID REFERENCES profiles(id),
    interviewer_notes TEXT,
    status TEXT, -- scheduled, completed, cancelled, rescheduled
    result TEXT, -- pending, accepted, rejected, no_show
    result_notes TEXT,
    decided_by UUID REFERENCES profiles(id),
    decided_at TIMESTAMPTZ,
    created_by UUID REFERENCES profiles(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### **3. جدول المقبولين**

```sql
CREATE TABLE membership_accepted_members (
    id UUID PRIMARY KEY,
    application_id UUID REFERENCES membership_applications(id),
    interview_id UUID REFERENCES membership_interviews(id),
    full_name TEXT NOT NULL,
    email TEXT NOT NULL,
    phone TEXT,
    assigned_committee TEXT NOT NULL,
    member_number TEXT UNIQUE, -- يتم توليده تلقائياً
    join_date TIMESTAMPTZ DEFAULT NOW(),
    status TEXT, -- active, inactive, suspended, terminated
    notes TEXT,
    added_by UUID REFERENCES profiles(id),
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### **4. دالة توليد رقم العضوية**

```sql
CREATE FUNCTION generate_member_number() RETURNS TEXT
-- صيغة الرقم: ADYY0001
-- AD = اختصار أديب
-- YY = آخر رقمين من السنة
-- 0001 = رقم تسلسلي
```

---

## 📂 هيكل القائمة المنسدلة

```
👤 إدارة العضوية ▼
    ├─ ⚙️ إعدادات التسجيل (مستوى 8+)
    ├─ 📋 اللجان المتاحة (مستوى 8+)
    ├─ 👁️ طلبات العضوية (مستوى 5+) - عرض فقط
    ├─ ✅ مراجعة الطلبات (مستوى 8+) - 3 خيارات
    ├─ 📅 المقابلات (مستوى 7+) - جدولة وقبول/رفض
    ├─ 👥 المقبولين (مستوى 5+) - قائمة الأعضاء
    └─ 🗂️ أرشيف التسجيل (مستوى 7+)
```

---

## 🔄 سير العمل الكامل

### **1. استلام الطلب**
```
طلب جديد → حالة: new
```

### **2. مراجعة الطلب (مستوى 8+)**
```
قسم "مراجعة الطلبات"
↓
3 خيارات:
├─ قبول للمقابلة → حالة: approved_for_interview
├─ رفض → حالة: rejected (نهائي)
└─ مراجعة → حالة: under_review
```

### **3. جدولة المقابلة (مستوى 7+)**
```
قسم "المقابلات"
↓
- اختيار المتقدم (من المقبولين للمقابلة)
- تحديد التاريخ والوقت
- اختيار النوع (حضوري/أونلاين/هاتفي)
- إضافة الموقع أو رابط الاجتماع
↓
حالة المقابلة: scheduled
```

### **4. إجراء المقابلة**
```
عرض تفاصيل المقابلة
↓
2 خيارات:
├─ قبول → يضاف إلى جدول المقبولين + رقم عضوية
└─ رفض → حالة المقابلة: rejected
```

### **5. العضو المقبول**
```
جدول membership_accepted_members
↓
- رقم عضوية تلقائي (مثال: AD240001)
- تعيين اللجنة
- حالة: active
```

---

## 🎨 الواجهات

### **قسم مراجعة الطلبات**

**الإحصائيات:**
- طلبات جديدة
- قيد المراجعة
- مقبولة
- مرفوضة

**الجدول:**
- الاسم
- البريد الإلكتروني
- اللجنة المرغوبة
- الحالة
- تاريخ التقديم
- زر "عرض" → نافذة التفاصيل مع 3 أزرار

### **قسم المقابلات**

**الإحصائيات:**
- مقابلات مجدولة
- معلقة
- مقبولين
- مرفوضين

**الجدول:**
- المتقدم
- اللجنة
- تاريخ المقابلة
- النوع (حضوري/أونلاين/هاتفي)
- الحالة
- النتيجة
- زر "عرض" → نافذة التفاصيل مع أزرار القبول/الرفض

**زر "جدولة مقابلة جديدة":**
- نافذة منبثقة لإدخال التفاصيل
- يعرض فقط المتقدمين المقبولين للمقابلة

### **قسم المقبولين**

**الإحصائيات:**
- إجمالي الأعضاء
- نشط
- غير نشط
- انضموا هذا الشهر

**الجدول:**
- رقم العضوية
- الاسم
- البريد الإلكتروني
- اللجنة
- تاريخ الانضمام
- الحالة
- زر "عرض" → نافذة التفاصيل

---

## 🔐 الصلاحيات

| القسم | المستوى | القراءة | الكتابة | الحذف |
|------|---------|---------|---------|--------|
| إعدادات التسجيل | 8+ | ✅ | ✅ | ❌ |
| اللجان المتاحة | 8+ | ✅ | ✅ | ❌ |
| طلبات العضوية (عرض) | 5+ | ✅ | ❌ | ❌ |
| مراجعة الطلبات | 8+ | ✅ | ✅ | ❌ |
| المقابلات | 7+ | ✅ | ✅ | ❌ |
| المقبولين | 5+ | ✅ | ❌ | ❌ |
| الأرشيف | 7+ | ✅ | ✅ | 10 |

---

## 📊 الإحصائيات المتوفرة

### **في كل قسم:**
- إحصائيات حية ومحدثة
- فلترة حسب الحالة
- بحث متقدم
- تصدير البيانات

### **في قسم المقابلات:**
- عدد المقابلات المجدولة
- المقابلات المعلقة
- معدل القبول
- معدل الرفض

### **في قسم المقبولين:**
- إجمالي الأعضاء
- توزيع حسب اللجان
- الأعضاء الجدد شهرياً
- حالة العضوية

---

## 🚀 كيفية الاستخدام

### **للموارد البشرية (مستوى 8+):**

#### **1. مراجعة طلب جديد:**
```
1. افتح "مراجعة الطلبات"
2. اضغط "عرض" على أي طلب
3. راجع التفاصيل الكاملة
4. اختر أحد الخيارات:
   - قبول للمقابلة (أضف ملاحظات اختيارية)
   - رفض (أضف سبب الرفض)
   - مراجعة (لمزيد من الدراسة)
```

#### **2. جدولة مقابلة:**
```
1. افتح "المقابلات"
2. اضغط "جدولة مقابلة جديدة"
3. اختر المتقدم من القائمة
4. حدد التاريخ والوقت
5. اختر نوع المقابلة
6. أضف الموقع أو رابط الاجتماع
7. اضغط "جدولة"
```

#### **3. تسجيل نتيجة المقابلة:**
```
1. افتح "المقابلات"
2. اضغط "عرض" على المقابلة
3. راجع معلومات المتقدم
4. اختر:
   - قبول (أضف ملاحظات اختيارية)
   - رفض (أضف سبب الرفض)
```

### **للمستخدمين (مستوى 5+):**

#### **عرض الطلبات:**
```
1. افتح "طلبات العضوية"
2. استعرض جميع الطلبات
3. استخدم البحث والفلترة
4. صدّر البيانات عند الحاجة
```

#### **عرض المقبولين:**
```
1. افتح "المقبولين"
2. استعرض قائمة الأعضاء
3. ابحث برقم العضوية أو الاسم
4. اضغط "عرض" لتفاصيل أي عضو
```

---

## 📁 الملفات المعدّلة/المُنشأة

### **قاعدة البيانات:**
- ✅ `supabase/migrations/035_create_interviews_system.sql` (جديد)

### **HTML:**
- ✅ `admin/dashboard.html`
  - قسم المقابلات (جديد)
  - قسم المقبولين (جديد)

### **JavaScript:**
- ✅ `admin/dashboard.js`
  - تحديث القائمة المنسدلة
  - إضافة معالجات للأقسام الجديدة

- ✅ `admin/js/membership-manager.js`
  - `viewApplication()` - محدثة لـ 3 خيارات
  - `approveForInterview()` - جديدة
  - `rejectApplication()` - جديدة
  - `markUnderReview()` - جديدة
  - `loadInterviews()` - جديدة
  - `viewInterview()` - جديدة
  - `acceptInterview()` - جديدة
  - `rejectInterview()` - جديدة
  - `scheduleNewInterview()` - جديدة
  - `loadAcceptedMembers()` - جديدة
  - `viewAcceptedMember()` - جديدة

---

## ⚙️ التثبيت والتشغيل

### **1. تطبيق Migration:**
```bash
cd supabase
supabase db push
```

### **2. التحقق من الجداول:**
```sql
-- التحقق من جدول المقابلات
SELECT * FROM membership_interviews;

-- التحقق من جدول المقبولين
SELECT * FROM membership_accepted_members;

-- اختبار دالة توليد رقم العضوية
SELECT generate_member_number();
```

### **3. الاستخدام:**
```
1. افتح لوحة التحكم
2. اذهب إلى "إدارة العضوية"
3. جرّب الأقسام الجديدة
4. اختبر سير العمل الكامل
```

---

## ⚠️ ملاحظات مهمة

1. **رقم العضوية تلقائي:** يتم توليده عند قبول المتقدم بعد المقابلة
2. **الحالات غير قابلة للتراجع:** الرفض نهائي في كلا المرحلتين
3. **الصلاحيات محكمة:** كل قسم له RLS policies مستقلة
4. **البيانات مترابطة:** حذف طلب يحذف المقابلة والعضوية المرتبطة (CASCADE)
5. **الأداء محسّن:** فهارس على جميع الأعمدة المهمة

---

## 🔧 الصيانة

### **استعلامات مفيدة:**

```sql
-- عدد المقابلات المجدولة
SELECT COUNT(*) FROM membership_interviews WHERE status = 'scheduled';

-- عدد الأعضاء النشطين
SELECT COUNT(*) FROM membership_accepted_members WHERE status = 'active';

-- آخر 10 أعضاء انضموا
SELECT * FROM membership_accepted_members 
ORDER BY join_date DESC LIMIT 10;

-- معدل القبول في المقابلات
SELECT 
    COUNT(*) FILTER (WHERE result = 'accepted') * 100.0 / COUNT(*) as acceptance_rate
FROM membership_interviews 
WHERE result IS NOT NULL;
```

---

## ✅ الخلاصة

النظام الجديد يوفر:
- ✅ مراحل قبول متعددة ومنظمة
- ✅ نظام مقابلات متكامل مع جدولة
- ✅ توليد تلقائي لأرقام العضوية
- ✅ إدارة كاملة للأعضاء المقبولين
- ✅ صلاحيات محكمة لكل مرحلة
- ✅ إحصائيات شاملة ومفصلة
- ✅ سير عمل واضح ومنطقي
- ✅ واجهات سهلة ومنظمة

**تم التطوير بعناية فائقة لضمان عدم حدوث أخطاء والحفاظ على سلامة البيانات.** 🎉

---

## 📞 الدعم

في حالة وجود مشاكل:
1. تحقق من تطبيق migration بنجاح
2. راجع console logs في المتصفح
3. تأكد من صلاحيات المستخدم
4. تحقق من RLS policies في Supabase
