-- =============================================
-- إضافة حقل رابط الموقع (Google Maps) للأنشطة
-- =============================================
-- السياق: نموذج الإدارة بات يطلب رابط Google Maps للموقع
-- بالإضافة إلى اسم/عنوان المكان النصي.

ALTER TABLE activities
    ADD COLUMN IF NOT EXISTS location_url TEXT;
