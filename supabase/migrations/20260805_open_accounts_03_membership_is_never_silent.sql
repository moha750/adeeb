-- ══════════════════════════════════════════════════════════════════════════════
-- العضويّةُ لا تُمنح صمتًا
--
-- كان `joined_date` افتراضُه `CURRENT_DATE` — وكان صوابًا يوم كان الجدولُ للأعضاء
-- وحدهم. وقد صار حدَّ العضويّة بعد التوحيد (م١)، فبقاءُ الافتراض يعني أنّ **كلّ
-- إدراجٍ ينسى العمود يصنع عضوًا** — وأكثرُ صفوف هذا الجدول من اليوم فصاعدًا لأناسٍ
-- لم ينضمّوا. فالافتراضُ يُنزع: من أدرج صفًّا قال صراحةً أعضوٌ هو أم صاحبُ حساب.
--
-- (`create_my_account_profile` تكتب `null` صراحةً، وصانعُ العضو يكتب تاريخَه.)
-- ══════════════════════════════════════════════════════════════════════════════
alter table public.profiles alter column joined_date drop default;
