-- ============================================================
-- إسقاط roles.role_category — بند ٤ في V1-DEATH-CHECKLIST
-- عمود ميّت NOT NULL: صفر دالّة تقرؤه، صفر مقارنة بقيمته، يعترض كلّ
-- منصب جديد ويناقض council_type. كان محجوزًا بسطرَي select في V1
-- (نافذتا مشاركة الاستبيانات) — كسرهما مقبول بقرار المالك (2026-07-16)،
-- ونافذة المشاركة نفسها تقاعدت مع survey_sharing في هجرة التقليم.
-- داخل القاعدة: idx_roles_category وحده يعتمد عليه ويسقط معه.
-- ============================================================
alter table roles drop column if exists role_category;
