# نظام إدارة نادي أدِيب - نظرة شاملة

## 📖 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [البنية التقنية](#البنية-التقنية)
3. [قاعدة البيانات](#قاعدة-البيانات)
4. [نظام الصلاحيات](#نظام-الصلاحيات)
5. [الملفات والمجلدات](#الملفات-والمجلدات)
6. [الوظائف الرئيسية](#الوظائف-الرئيسية)

## 🎯 نظرة عامة

نظام إدارة نادي أدِيب هو نظام متكامل مبني بالكامل على تقنيات الويب الحديثة، مصمم خصيصاً لتلبية احتياجات الهيكل التنظيمي لنادي أدِيب.

### الأهداف الرئيسية

- ✅ **إدارة مركزية**: نظام موحد لإدارة جميع جوانب النادي
- ✅ **صلاحيات متدرجة**: نظام صلاحيات يعكس الهيكل التنظيمي
- ✅ **سهولة الاستخدام**: واجهات بديهية باللغة العربية
- ✅ **الأمان**: حماية متقدمة للبيانات
- ✅ **قابلية التوسع**: سهولة إضافة مميزات جديدة

## 🏗️ البنية التقنية

### Frontend (الواجهة الأمامية)

```
التقنيات المستخدمة:
├── HTML5
├── CSS3 (تصميم مخصص)
├── JavaScript (Vanilla JS)
├── Font Awesome (الأيقونات)
└── Chart.js (الرسوم البيانية)
```

### Backend (الخادم الخلفي)

```
المنصة:
└── Supabase
    ├── PostgreSQL (قاعدة البيانات)
    ├── Authentication (المصادقة)
    ├── Row Level Security (الأمان)
    └── Real-time Subscriptions (التحديثات الفورية)
```

### المميزات التقنية

- **Progressive Web App (PWA)**: يمكن تثبيته كتطبيق
- **Responsive Design**: يعمل على جميع الأجهزة
- **Real-time Updates**: تحديثات فورية للبيانات
- **Offline Support**: إمكانية العمل بدون إنترنت (جزئياً)

## 🗄️ قاعدة البيانات

### الجداول الرئيسية (17 جدول)

#### 1. جداول المستخدمين والأدوار
```
profiles (الملفات الشخصية)
├── id (UUID)
├── username
├── full_name
├── email
├── phone
├── avatar_url
├── bio
├── account_status
└── joined_date

roles (الأدوار)
├── id
├── role_name
├── role_name_ar
├── role_level (1-10)
├── role_category
└── description

user_roles (ربط المستخدمين بالأدوار)
├── id
├── user_id
├── role_id
├── committee_id
├── is_active
└── assigned_at
```

#### 2. جداول اللجان والمشاريع
```
committees (اللجان)
├── id
├── committee_name_ar (المعرف الفريد)
├── description
└── is_active

projects (المشاريع)
├── id
├── project_name
├── description
├── committee_id
├── project_leader
├── status
├── priority
├── start_date
├── end_date
└── budget
```

#### 3. جداول المهام
```
tasks (المهام)
├── id
├── title
├── description
├── project_id
├── committee_id
├── assigned_to
├── assigned_by
├── status
├── priority
├── due_date
└── completed_at

task_comments (تعليقات المهام)
├── id
├── task_id
├── user_id
├── comment
└── created_at

task_attachments (مرفقات المهام)
├── id
├── task_id
├── file_name
├── file_url
└── uploaded_by
```

#### 4. جداول الاجتماعات
```
meetings (الاجتماعات)
├── id
├── title
├── description
├── committee_id
├── meeting_type
├── scheduled_at
├── duration_minutes
├── location
├── meeting_link
└── status

attendance (الحضور)
├── id
├── user_id
├── meeting_id
├── status
└── notes
```

#### 5. جداول التقييم والتقارير
```
member_evaluations (تقييمات الأعضاء)
├── id
├── user_id
├── evaluator_id
├── committee_id
├── evaluation_period_start
├── evaluation_period_end
├── performance_score
├── attendance_score
├── quality_score
├── teamwork_score
├── overall_score
├── strengths
└── areas_for_improvement

reports (التقارير)
├── id
├── report_title
├── report_type
├── committee_id
├── generated_by
├── report_data (JSONB)
├── period_start
└── period_end
```

#### 6. جداول النظام
```
permissions (الصلاحيات)
├── id
├── permission_name
├── permission_name_ar
├── description
└── category

role_permissions (ربط الأدوار بالصلاحيات)
├── id
├── role_id
├── permission_id
├── can_create
├── can_read
├── can_update
└── can_delete

notifications (الإشعارات)
├── id
├── user_id
├── title
├── message
├── notification_type
├── is_read
└── action_url

activity_log (سجل الأنشطة)
├── id
├── user_id
├── action_type
├── target_type
├── target_id
├── details (JSONB)
└── ip_address

announcements (الإعلانات)
├── id
├── title
├── content
├── announcement_type
├── target_audience
├── committee_id
├── is_pinned
├── published_at
└── expires_at
```

### العلاقات بين الجداول

```
profiles (1) ←→ (N) user_roles
user_roles (N) ←→ (1) roles
user_roles (N) ←→ (1) committees
roles (1) ←→ (N) role_permissions
role_permissions (N) ←→ (1) permissions
projects (N) ←→ (1) committees
projects (N) ←→ (1) profiles (project_leader)
tasks (N) ←→ (1) projects
tasks (N) ←→ (1) committees
tasks (N) ←→ (1) profiles (assigned_to)
tasks (N) ←→ (1) profiles (assigned_by)
meetings (N) ←→ (1) committees
attendance (N) ←→ (1) meetings
attendance (N) ←→ (1) profiles
```

### الدوال المساعدة (Functions)

```sql
-- الحصول على صلاحيات المستخدم
get_user_permissions(user_uuid UUID)

-- الحصول على أعلى مستوى دور للمستخدم
get_user_highest_role_level(user_uuid UUID)

-- التحقق من صلاحية معينة
check_user_permission(user_uuid UUID, perm_name TEXT, action_type TEXT)

-- تسجيل النشاط
log_activity(p_user_id UUID, p_action_type TEXT, ...)

-- تحديث updated_at تلقائياً
update_updated_at_column()
```

### طرق العرض (Views)

```sql
-- معلومات المستخدمين مع أدوارهم
user_roles_view

-- المهام مع معلومات كاملة
tasks_detailed_view

-- المشاريع مع معلومات كاملة
projects_detailed_view
```

## 🔐 نظام الصلاحيات

### مستويات الصلاحيات (10 مستويات)

```
المستوى 10: رئيس النادي
├── صلاحيات كاملة
├── إدارة النظام
└── الوصول لكل شيء

المستوى 9: قادة المجلس الأعلى
├── إدارة الموارد البشرية
├── إدارة الجودة
└── تقارير شاملة

المستوى 8: أعضاء إداريون / رئيس المجلس الإداري
├── إدارة اللجان
├── إدارة المستخدمين
└── الموافقات

المستوى 7: قادة اللجان
├── إدارة اللجنة
├── تعيين المهام
└── إنشاء مشاريع

المستوى 6: نواب قادة اللجان
├── مساعدة القائد
├── تعيين مهام
└── متابعة الإنجاز

المستوى 5: أعضاء اللجان
├── تنفيذ المهام
├── تحديث الحالة
└── رفع الأعمال

المستوى 1: أعضاء عاديون
└── عرض الإعلانات فقط
```

### نظام CRUD للصلاحيات

كل صلاحية تحتوي على 4 أذونات:
- **Create**: إنشاء جديد
- **Read**: قراءة/عرض
- **Update**: تحديث/تعديل
- **Delete**: حذف

### Row Level Security (RLS)

جميع الجداول محمية بسياسات RLS:
- المستخدم يرى بياناته فقط
- أو بيانات لجنته
- أو بيانات يسمح له دوره برؤيتها

## 📁 الملفات والمجلدات

### الهيكل العام

```
adeeb-main/
├── admin/                      # نظام الإدارة الجديد
│   ├── login.html             # صفحة تسجيل الدخول
│   ├── dashboard.html         # لوحة التحكم الرئيسية
│   ├── auth-manager.js        # نظام إدارة المصادقة
│   ├── dashboard.js           # منطق لوحة التحكم
│   ├── auth.css              # تصميم صفحات المصادقة
│   ├── dashboard.css         # تصميم لوحة التحكم
│   └── README.md             # دليل الاستخدام
│
├── supabase/                  # إعدادات قاعدة البيانات
│   └── migrations/
│       ├── 001_initial_schema.sql    # الجداول والعلاقات
│       ├── 002_rls_policies.sql      # سياسات الأمان
│       └── 003_seed_data.sql         # البيانات الأولية
│
├── supabase-config.js         # إعدادات الاتصال
├── SETUP_GUIDE.md            # دليل الإعداد
├── SYSTEM_OVERVIEW.md        # هذا الملف
└── README.md                 # ملف README الرئيسي
```

### الملفات الرئيسية

#### 1. admin/login.html
صفحة تسجيل الدخول للنظام الإداري
- تصميم احترافي
- التحقق من البيانات
- رسائل خطأ بالعربية
- إظهار/إخفاء كلمة المرور

#### 2. admin/dashboard.html
لوحة التحكم الرئيسية
- هيدر ثابت
- قائمة جانبية ديناميكية
- محتوى رئيسي متعدد الأقسام
- نافذة الإشعارات
- قائمة المستخدم

#### 3. admin/auth-manager.js
نظام إدارة المصادقة والصلاحيات
- التحقق من تسجيل الدخول
- الحصول على الأدوار
- التحقق من الصلاحيات
- حماية الصفحات
- تسجيل الأنشطة

#### 4. admin/dashboard.js
منطق لوحة التحكم
- تحميل البيانات
- بناء القائمة الديناميكية
- الرسوم البيانية
- الإحصائيات
- النشاطات الأخيرة

#### 5. supabase-config.js
إعدادات الاتصال بـ Supabase
- URL المشروع
- المفتاح العام
- إعدادات المصادقة

## ⚙️ الوظائف الرئيسية

### 1. نظام المصادقة

```javascript
// تسجيل الدخول
await sb.auth.signInWithPassword({ email, password })

// التحقق من الجلسة
await sb.auth.getSession()

// تسجيل الخروج
await sb.auth.signOut()
```

### 2. إدارة الأدوار

```javascript
// الحصول على دور المستخدم
await AuthManager.getUserRole(userId)

// التحقق من مستوى الصلاحية
await AuthManager.checkRoleLevel(userId, minLevel)

// التحقق من صلاحية معينة
await AuthManager.checkPermission(userId, permissionName, actionType)
```

### 3. إدارة المستخدمين

```javascript
// إضافة مستخدم جديد
await sb.from('profiles').insert({ ... })

// تعيين دور
await sb.from('user_roles').insert({ user_id, role_id, committee_id })

// تحديث حالة الحساب
await sb.from('profiles').update({ account_status: 'active' })
```

### 4. إدارة المهام

```javascript
// إنشاء مهمة
await sb.from('tasks').insert({
    title,
    description,
    assigned_to,
    committee_id,
    status: 'pending',
    priority: 'medium'
})

// تحديث حالة المهمة
await sb.from('tasks').update({ status: 'completed' })

// إضافة تعليق
await sb.from('task_comments').insert({ task_id, user_id, comment })
```

### 5. إدارة المشاريع

```javascript
// إنشاء مشروع
await sb.from('projects').insert({
    project_name,
    committee_id,
    project_leader,
    status: 'planning'
})

// تحديث المشروع
await sb.from('projects').update({ status: 'active' })
```

### 6. الإشعارات

```javascript
// إنشاء إشعار
await sb.from('notifications').insert({
    user_id,
    title,
    message,
    notification_type: 'task'
})

// تحديد كمقروء
await sb.from('notifications').update({ is_read: true })
```

### 7. التقارير

```javascript
// إنشاء تقرير
await sb.from('reports').insert({
    report_title,
    report_type,
    report_data: { ... },
    period_start,
    period_end
})
```

## 🔄 تدفق البيانات

### تسجيل الدخول

```
1. المستخدم يدخل البريد وكلمة المرور
2. التحقق من البيانات في Supabase Auth
3. جلب الملف الشخصي من profiles
4. التحقق من account_status
5. جلب الأدوار من user_roles
6. التحقق من role_level >= 5
7. تسجيل النشاط في activity_log
8. التوجيه إلى لوحة التحكم
```

### إنشاء مهمة

```
1. المستخدم يملأ نموذج المهمة
2. التحقق من الصلاحيات (can_create)
3. إدخال المهمة في جدول tasks
4. إنشاء إشعار للمسند إليه
5. تسجيل النشاط
6. تحديث الواجهة
```

### تحديث حالة المهمة

```
1. المستخدم يغير حالة المهمة
2. التحقق من الصلاحيات (can_update)
3. تحديث الحالة في جدول tasks
4. إشعار المسند منه بالتحديث
5. تسجيل النشاط
6. تحديث الإحصائيات
```

## 🎨 التصميم والواجهات

### نظام الألوان

```css
--main-blue: #274060      /* الأزرق الرئيسي */
--accent-blue: #3d8fd6    /* الأزرق الفاتح */
--gold: #d4af37           /* الذهبي */
--success: #10b981        /* الأخضر */
--error: #ef4444          /* الأحمر */
--warning: #f59e0b        /* البرتقالي */
```

### المكونات الرئيسية

- **البطاقات (Cards)**: عرض المعلومات
- **الجداول (Tables)**: عرض البيانات المجدولة
- **النماذج (Forms)**: إدخال البيانات
- **الأزرار (Buttons)**: الإجراءات
- **الإشعارات (Notifications)**: التنبيهات
- **الرسوم البيانية (Charts)**: الإحصائيات

### الاستجابة (Responsive)

```
Desktop: > 1024px
Tablet: 768px - 1024px
Mobile: < 768px
```

## 🚀 الأداء والتحسينات

### التحسينات المطبقة

- ✅ Lazy Loading للصور
- ✅ Code Splitting
- ✅ Caching للبيانات
- ✅ Debouncing للبحث
- ✅ Pagination للجداول
- ✅ Compression للملفات

### الأمان

- ✅ Row Level Security
- ✅ HTTPS فقط
- ✅ CSRF Protection
- ✅ XSS Prevention
- ✅ SQL Injection Prevention
- ✅ Rate Limiting

## 📈 المستقبل والتطوير

### المميزات المخططة

- [ ] تطبيق موبايل (React Native)
- [ ] نظام الدردشة الداخلي
- [ ] تكامل مع التقويم
- [ ] تصدير التقارير PDF
- [ ] نظام الملفات المشتركة
- [ ] لوحات تحكم مخصصة
- [ ] تحليلات متقدمة
- [ ] API عام للمطورين

---

**نادي أدِيب** - نظام إدارة متكامل ومتطور
تم التطوير بواسطة فريق نادي أدِيب التقني
الإصدار 1.0.0 - يناير 2026
