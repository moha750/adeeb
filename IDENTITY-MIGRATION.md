# سجلّ ترحيل الهوية — من اللوح إلى الإنتاج

يتتبّع هذا الملفّ ما نُقل من **لوح الهوية** (`v2/packages/design-system/identity-board.html`)
إلى ملفّات الإنتاج (`tokens.css` / `globals.css`) التي تستهلكها لوحة التحكّم، وما زال معلّقًا على اللوح.

**القاعدة:** لا يُنقل إلّا ما استقرّ. وما زال يُنقّح يبقى على اللوح، ويُنقل مكوّنًا مكوّنًا عند استقراره
(ثمّ يُحذف تعديله من اللوح لئلّا يوجد مصدرٌ مزدوج).

---

## 📋 لوحة الحالة — ما رُحّل وما لم يُرحَّل (نراجعها معًا)

آخر تحديث: 2026-07-10 · الرمز: ✅ مُرحَّل · ⏳ لم يُرحَّل بعد · 🎨 بديل على اللوح فقط · «مطابق؟» = نسخة الإنتاج تطابق اللوح حرفيًّا.

| القسم (على اللوح) | الحالة | مطابق؟ | أين في الإنتاج / ملاحظة |
|---|---|---|---|
| الأسس · اللون · Aurora · الطباعة · الشكل · الظلال · الحركة | ✅ | ✅ | `tokens.css` (الرموز) |
| الهيكل (`.ash-*`) | ✅ | ✅ | `DashboardShell` (أيقونة الخروج → Phosphor) |
| الأزرار (`.abtn-*`) | ✅ | ✅ | `Button` (حُذف inverse الدلاليّ) |
| التنبيهات (`.aalert`/`.toast`) · الشارات (`.abadge-*`) | ✅ | ✅ | `Alert`/`ToastProvider`/`Badge` |
| البطاقة العامّة + نغماتها (`.acard-tone-*`) | ✅ | ✅ | `Card` (خاصّيّة `tone`) — حُذف outline/tinted |
| كرت العضو (`.mc-tone-*`) | ✅ | ✅ | `MemberCard` (نغمة بالحالة؛ النشط = العلامة) |
| الإحصائيات (`.stat`) + قاعدة الشبكة | ✅ | ✅ | `.stat-grid` (سقف 3 · الوحيد/اليتيم يمتدّ) |
| الجدول (`.dt-*`) + نغماته | ✅ | ✅ | `DataTable` (`tone`/`rowTone` · أفتار منغَّم) |
| القوائم (`.dm-*`) + نغماتها | ✅ | ✅ | `DropdownMenu` (خاصّيّة `tone`) |
| النافذة · عرض البيانات (`.pvb`/`.pva`) | ✅ | ✅ | `Modal` (`hero`) — **أنميشن النسخ أُعيد بعد مراجعتك** |
| النافذة · الاستثنائيّة (`.mdl-alert`) | ✅ | ✅ | `ConfirmDialog` (نغمة + أيقونة متحرّكة) |
| النافذة · دورة الحياة + التذييل الهدّام | ✅ | — | حركة دخول/خروج + زرّ حذف في التعديل (إضافة) |
| التبويبات · الحالات · الأفتار | ✅ | ✅ | `Tabs`/`EmptyState`/`Avatar` |
| RTL شامل لمكوّنات Radix | ✅ | — | `DirectionProvider` في `layout.tsx` |
| الحقول (`.fld-*`) | ✅ | ✅ | `Field`/`Select`/`Input`/`Textarea` → تسمية ثابتة أعلى + أيقونة هوية · حالات 600 |
| **شريط الأدوات · المرشّح** (`.asel`) | ⏳ | — | `FilterSelect` يعيد اختراع منسدل — يُستبدَل بـ`.asel`/`DropdownMenu` |
| عرض البيانات · التصميم البديل (أفتار مستدير) | 🎨 | — | بديل على اللوح فقط — لم نختره للتطبيق |

**🧹 تنظيف مؤجّل:** CSS ميّت في `.org-*` · المكرّرات في جسم اللوح (نسخ `pvb`/`pva`/`mdl-alert` مزدوجة — **أُلغي حذفها بطلبك**).

**نظام الحقل — رُحّل حرفيًّا (2026-07-11):** استُبدل `.af-*` (تسمية عائمة) بـ`.fld-*` (تسمية ثابتة أعلى + أيقونة هوية فولاذيّة + سطح مسطّح + تركيز فولاذيّ + استدارة 16 + حالات الخطأ/النجاح بلون 600) — منقولًا حرفيًّا من اللوح. أُعيد بناء `Field`/`Select`/`Input`/`Textarea`، ووُصِلت أيقونات الهوية في نموذج العضو والاعتمادات. typecheck ✓، ومحقّق بصريًّا على اللوح §9.

**نتيجة المراجعة الحرفيّة (2026-07-10):** النوافذ طُوبقت حرفًا حرفًا مع اللوح — الانحراف الوحيد كان تبسيط أنميشن النسخ (أُصلح)، ونُظّفت 4 بقايا ميتة من `.pv-*` القديم. باقي المُرحَّل مصدره `globals.css` مباشرةً (بلا نسخة مزدوجة) فلا انحراف ممكن.

---

## ✅ مُنقَل إلى الإنتاج

### 2026-07-09 — الأساسات (`tokens.css`)
- **الألوان الدلاليّة:** مقاييس تونيّة كاملة (success/warning/danger من 50 إلى 900)، والأساس = **600**، والأصناف الناعمة = **50**.
- **التدرّجات:** تدرّج هوية **واحد** (`steel-400 → navy-800`)؛ `primary/strong/hover/info` كلّها تشير إليه. الدلاليّة الثلاثة = `linear-gradient(135deg, tone-300, tone-600)`.
- **سطح Aurora:** `--surface-aurora` (تعبئة مسطّحة ~14% عبر `color-mix` — واعية بالثيم) و`--border-aurora`.
- **الاستدارة:** موحّدة **16** (`--radius` + كلّ `--radius-*` = 16؛ `--radius-full` يبقى 9999 للحبوب/الدوائر).
- **الظلّ:** واحد منغَّم — `--shadow-tone: 69,113,154` (فولاذيّ)، و`--shadow-sm/md/lg` = ظلّ النغمة الواحد.

### 2026-07-09 — تطبيق الأساسات (`globals.css` — «طبقة توحيد الهوية» في نهاية الملفّ)
- **استدارة 16** مفروضة (`!important`) على قائمة المكوّنات (أزرار/حقول/بطاقات/نافذة/أفاتار…).
- **سطح Aurora** على أسطح البطاقات والنافذة (`.acard/.mc/.gal-sec/.pv-card/.pv-hero/.mdl`)، مع إعادة تثبيت `.acard-outline/.acard-tinted`.
- **نظام الظلّ المنغَّم:** ظلّ واحد على كل عنصر ذي ظلّ + تعيين `--shadow-tone` لكلّ زرّ بنفسه (فولاذيّ افتراضًا، ويتغيّر للدلاليّ في الصلب/الشبحيّ/المعكوس).

### 2026-07-09 — كرت العضو المنغَّم (`globals.css` — طبقة الهوية)
- `.mc-tone-brand/success/warning/danger`: سطح Aurora + حدّ + ظلّ **بلون الحالة**، ترويسة بتدرّج الحالة (والنقش محفوظ)، أفاتار بتدرّجها، والاسم/الدور/الأيقونات/التذييل/صفوف البيانات بلون **600** (قيم البيانات تبقى محايدة). أُضيفت قاعدة `.mc-tone-danger .mc-role` الناقصة من اللوح.

### 2026-07-10 — تنقيح الأزرار + شريط التمرير (`tokens.css` + `globals.css`)
- **الشبحيّ المحايد واعٍ بالثيم:** رمزان جديدان في `tokens.css` (`--btn-ghost-fg` / `--btn-ghost-border`) = **فولاذيّ (steel-600)** في الفاتح و**أبيض** في الداكن (كـ«معكوس شبحيّ»)، فيتكيّف في أيّ ثيم حتى الحاويات المتداخلة. (كان مثبَّتًا على الكحليّ فيختفي في الداكن.)
- **حذف المعكوس الدلاليّ:** أُزيلت `.abtn-inverse-danger/success/warning` (زائدة — الألوان الصارخة تُقرأ على الداكن كما هي). بقي `.abtn-inverse` (أبيض) و`.abtn-inverse-ghost` فقط. عدد أنماط الزرّ 13 ← **10**.
- **حالة التحميل:** `.abtn[aria-busy="true"]` غير تفاعليّة (تمنع الإرسال المزدوج) وتبقى بلون الهوية — مميَّزة عن المعطّل الرماديّ. (الترميز يضيف `aria-busy="true"` لا `disabled`.)
- **الشبحيّ:** حدّ **2px**.
- **شريط التمرير — هوية «نحيل فولاذيّ»:** إبهام صلب رفيع **6px** حبّة كاملة، مجرى شفّاف، بلا مثلّثات أسهم (`::-webkit-scrollbar-button{display:none}`)، واعٍ بالثيم عبر `--color-secondary` (فولاذيّ 600 فاتح / 500 داكن). المكوّنات التي تُخفي شريطها عمدًا (`.ns-rail`/`.act-peek`) تبقى.
  - ⚠️ **علّة شريط الجذر:** وضع `scrollbar-width/scrollbar-color` القياسيّتين على `html` «يخطف» شريط الصفحة في كروميوم ويُلغي `::-webkit-scrollbar` عليه (فيبدو ثخينًا ولا يتأثّر بالتعديل — سبب «لم يتغيّر شيء»). الحلّ: عزل الخصائص القياسيّة لفايرفوكس وحده داخل `@supports (-moz-osx-font-smoothing: grayscale)` (كروميوم يتجاهله)، فيأخذ الجذرُ والصناديقُ تنسيق webkit الموحّد.

### 2026-07-10 — كرت الإحصائيات (KPI) + نُشر في لوحة التحكّم
- **المكوّن** (`globals.css`): `.stat` / `.stat-grid` / `.stat-ic` / `.stat-label` + نغمات `.stat-brand/-success/-warning/-danger`. سطح Aurora **بلون الحالة** (يُبدّل `--surface-aurora`+`--border-aurora`+`--shadow-tone` كالكروت المنغّمة)، أيقونة صلبة بتدرّج النغمة، رقم Eras، ومؤشّر الاتّجاه = **مكوّن `.abadge` الحقيقيّ** لا شارة مبتكرة.
- **الظلّ**: الظلّ الموحّد الواحد فقط (أُزيل ظلّ المرور المتضخّم — كان اجتهادًا يخالف قاعدة الظلّ الواحد).
- **المرور**: كِبَر موحّد عبر رمز جديد **`--hover-scale: 1.02`** (يشترك فيه `.mc:hover` و`.acard-interactive:hover` و`.stat:hover` — مصدر واحد) + تشديد الحدّ للنغمة + نبضة أيقونة انسيابيّة (`--ease-standard`, بلا ارتداد). حارس `prefers-reduced-motion`.
- **النشر**: `dashboard/page.tsx` (نظرة عامة) — رُقّيت الإحصائيات البدائيّة إلى كروت `.stat` (مكوّن خادم، SVG مضمّنة، بلا حدود عميلة). typecheck ✓.

### 2026-07-10 — منظومة نغمات الجدول (`globals.css` + `DataTable.tsx`)
- **صفوف منغّمة بحالتها** (`.dt-row.dt-tone-success/-warning/-danger`): تِنت خفيف على الخلايا (7–8%)، يشتدّ عند المرور (10–11%)، ويتجاوز التزيبير.
- **الجدول كلّه بنغمة** (`.dt.dt-tone-brand/-success/-warning/-danger`): متغيّر واحد **`--dt-accent`** يقود التزيبير والتحويم، والنغمة تبدّله + `--shadow-tone` + لون الحدّ + ترويسة بلون النغمة (`.dt[class*="dt-tone-"] .dt-head`). أربع نغمات، بلا قواعد مكرّرة.
- **أفتار الجدول يتبنّى النغمة** (تعبئة بتدرّج الحالة + ظلّ بلونها) — للطاولة كاملةً وللصفّ المفرد، كأفتار كرت العضو. ⚠️ وُضِع **قبل** طبقة التوحيد كي يظهر في اللوح ويعمل في التطبيق (ما بعد علامة «توحيد الهوية» يُقصّ من شريحة اللوح).
- **تمرير أنعم**: انتقال `background-color` على `.dt-c` بـ`--dur`، وخُفِّضت شدّة المرور (‎11%→8.5%‎، والمنغّم ‎13/14%→10/11%‎).
- **الوصل**: `DataTable.tsx` — خاصّيّتا **`tone`** (الطاولة كاملةً) و**`rowTone(row)`** (نغمة الصفّ حسب حالته). typecheck ✓.

### 2026-07-10 — البطاقة العامّة: نغمات + تشذيب + توحيد stroke (`globals.css` + `Card.tsx` + صفحات المعرض)
- **بطاقة عامّة منغّمة** (`.acard-tone-brand/-success/-warning/-danger`): سطح Aurora + حدّ + ظلّ + قرص الرأس بلون الحالة؛ **النصّ محايد** لمقروئيّة المحتوى. مبنيّة على `--surface-aurora`/`--border-aurora`/`--shadow-tone` (يقرؤها أساس `.acard`).
- **حذف جذريّ** لـ `acard-outline` و`acard-tinted` من كامل النظام: `globals.css` (طبقتا المكوّن والتوحيد) + نوع `variant` في `Card.tsx` صار **`default | elevated`** فقط + 4 صفحات معرض تستعملهما. (كانتا الوحيدتين بلا Aurora.)
- **`acard-elevated`**: نُقل وهج Aurora إلى **الأسفل** (`linear-gradient(0deg…)`) ليظهر في الجسم تحت الصورة (كان الجسم يقع في منطقة تلاشي الوهج العلويّ فيبدو بلا تلوين).
- **توحيد stroke عبر كلّ البطاقات بلا استثناء**: المرجع = الإحصائيات وكرت العضو → محايد/علامة **20%**، نجاح **30%**، تحذير/خطر **32%** (كلّها عبر `--border-aurora`). صُحّح: `acard-elevated` (أُزيل حدّه الشفّاف — كان الاستثناء الوحيد)، و`acard-tone-brand` (26%→20%)، و`acard-tone-warning` (30%→32%).
- **الوصل**: `Card.tsx` — خاصّيّة **`tone`**. typecheck ✓.

### 2026-07-10 — تطبيق الهوية على لوحة التحكّم (مسح شامل + وصل)
مسح شامل لكلّ شاشات اللوحة (35 ملفًّا، 4 مدقّقين متوازين) ثمّ تطبيق. النتيجة: اللوحة **ملتزمة بنيويًّا** (نادرًا ما تُثبَّت ألوان في `.tsx`)، والانحرافات = «نغمة ضائعة» و«إعادة اختراع» و«هيكسات في طبقات قديمة».

- **وصل النغمة** (القدرات كانت في CSS بلا منفذ في الواجهة):
  - `DropdownMenu` → خاصّيّة `tone` (‏`dm-tone-*`)، و`DataTable` يمرّرها لقائمة الصفّ حسب نغمة الصفّ/الطاولة.
  - `DataTable` → خاصّيّتا `tone` (طاولة) و`rowTone(row)` (صفّ) [أُضيفتا سابقًا]، ووُصِلتا في `MembersView`: شاشة أحاديّة الحالة → الطاولة كلّها بنغمتها (الموقوفون→danger…)، والمختلط → نغمة كلّ صفّ.
  - `MemberCard` → نغمة الكرت من حالة العضو (كانت الموقوف فقط) عبر خريطة `status → mc-tone-*`.
  - `CredentialsView` → شارة `cred-badge` المحايدة استُبدلت بمكوّن `Badge` منغَّم بالحالة.
- **إصلاح عطب**: أُزيلت أنماط الزرّ المحذوفة `inverse-danger/-success/-warning` من `Button.tsx` والمعرض (كانت تُصيَّر مكسورة).
- **تصفير هيكسات الهيكلة/التحليلات** (طبقتا `.org-*`/`.st-*` بُنيتا قبل التوحيد): الذهبيّ الشارد `#b8860b`→كحليّ الهوية (`--navy-700`)، تدرّج المجلس الإداريّ المثبَّت→`--grad-primary`، `#b52f22`→`--danger` (×6)، `#ffd9d4`→`--danger-100`، `#166a43`→`--success-700`، وظلّ `.st-tip` المخصَّص→`--shadow-md`.
- **توحيد كروت KPI**: تحليلات (`.st-kpi` بأيقونات)→مكوّن `.stat` كامل؛ الهيكلة/التعيينات (`.org-stat` شريط ملخّص **بلا أيقونات**، لا يناسب كرت `.stat` القائد بالأيقونة) → وُحِّد بمنحه هوية الكرت في مقاسه (Aurora + `--border-aurora` + أرقام Eras + استدارة 16 + ظلّ منغَّم). حُذف `.st-kpi*` الميّت.
- **قرار — لوحة ألوان المخطّطات**: قُنِّنت `--chart-1..6` في `tokens.css` (مرتكزة على الهوية: فولاذيّ/كحليّ + 4 لكنات)، ووُصِلت في `StatsView`. نطاق مستقلّ معتمَد لتعدّد السلاسل. ⏳ الوضع الداكن للمخطّطات: متابعة.
- **قرار — المجلسان (تنفيذيّ/إداريّ)**: يبقيان على **تدرّج الهوية الواحد** (القاعدة المقدّسة: اللونان معًا دائمًا؛ تدرّج فولاذيّ‑منفرد يخالفها)، ويتمايزان **بالعنوان** لا باللون. أيّ تمايز إضافيّ يكون بعنصر (أيقونة/وسم) لا بلون علامة ثانٍ.
- **الوصل بالمكوّنات محقّق typecheck ✓ بعد كلّ مرحلة.** التحقّق البصريّ للوحة يتمّ بجلسة مدير (محروسة بـ`getCurrentAdmin`)، وأصناف الهوية نفسها محقّقة على اللوح.
- **RTL شامل لمكوّنات Radix**: أُضيفت اعتماديّة `@radix-ui/react-direction` ولُفَّت اللوحة بـ`<DirectionProvider dir="rtl">` في `layout.tsx` — فقائمة الإجراءات و`Select` والنافذة تتبع الاتّجاه العربيّ (كانت `align="end"` تُحسَب LTR فتُفتح يسارًا).
- **نافذة عرض البيانات المطوّرة (`.pvb-*`/`.pva-*`) — رُحّلت من اللوح إلى الإنتاج**: غلاف منقوش بتدرّج الهوية + أفتار مربّع بنقطة حالة + شبكة خلايا (أيقونة+تسمية+قيمة) بزرّ نسخ لكلّ خليّة. وُسِّع `Modal` بخاصّيّة **`hero`** (وضع الغلاف يستبدل الترويسة) + `className`؛ وأُعيدت كتابة نافذة «عرض الملف» في `MembersView` (أفتار بأحرف الاسم + نقطة بلون الحالة + شبكة خلايا شاملة + تذييل «تعديل»). حُذف `.pv-*` القديم ومكوّن `Info`. النقش عبر `/brand/pattern-white.svg`. typecheck ✓.
  - ⚠️ **درس**: لا تضع `*` داخل تعليق CSS — التسلسل `*/` يُغلقه مبكّرًا (عطّل البناء مرّة). وأخطاء CSS في Turbopack «لزجة» فتحتاج إعادة تشغيل الخادم لا تحميلًا ساخنًا.
- **النافذة الاستثنائيّة (تنبيه/تأكيد) — رُحّلت**: أنماط `.mdl-alert`/`.mdl-tone-*`/`.mdl-tic` — سطح Aurora بلون النغمة + أيقونة متحرّكة **بمعناها** (نجاح يُرسَم · تحذير يتأرجح · خطر يرتجف) + أزرار ممتدّة + حارس `prefers-reduced-motion`. مكوّن جديد **`ConfirmDialog`** (Radix Dialog)، ووُصِل **تأكيد حذف العضو** في `MembersView` (بدل التنبيه الفوريّ). typecheck ✓.
- **النافذة الأساسيّة — اكتملت دورة حياتها**: النواة (حركة دخول + تمرير المحتوى الطويل مع ترويسة/تذييل ثابتَين + سقف الشاشة) كانت في الإنتاج أصلًا؛ أُضيفت **حركة الخروج** (ربط `.mdl`/`.mdl-scrim` بـ`data-state` من Radix: خروج معكوس + حارس حركة) و**تذييل هدّام** في نافذة التعديل (زرّ «حذف العضو» يسارًا يفتح تأكيد الخطر). بهذا **نظام النافذة كامله في الإنتاج**. typecheck ✓.

**⏳ تنظيف مؤجّل (طفيف):** CSS ميّت في `.org-*` (‏`.org-slot*`/`.org-coord`/`.org-modal-*`)، واستدارات حرفيّة `12/14`→`--radius` في تداخل الهيكلة.

---

## ⏳ ما زال على اللوح (يُنقل عند استقراره)

- **نظام الحقل** (`.fld-*`: تسمية ثابتة أعلى + أيقونة هويّة بجانبها + حالات 600 + تركيز فولاذيّ).
  ⚠️ **يغيّر البنية (Markup):** اللوحة الآن تستعمل `.af-*` العائم؛ نقله يتطلّب تحديث ترميز التطبيق لا CSS فقط.
- **شريط الأدوات** (بحث ومرشّحات بسطح الحقل، المرشّح = مكوّن `.asel`، لوحة منسدلة موحّدة).
- **زرّ الإغلاق** (`.mdl-x`/`.tx`): تِنت شفّاف خفيف عاديًّا + حلقة عند المرور.

---

## 📝 ملاحظات ومتابعات

- **الوضع الداكن:** لم يُتجاوز `--shadow-tone` في الداكن → ظلال النغمة تستعمل الفولاذيّ في الداكن (خفيف). يُنظر لاحقًا في نغمة ظلّ داكنة.
- **مصدر مزدوج مؤقّت:** اللوح ما زال يحمل تعديلات الأساسات في `ib-overrides`؛ بعد التأكّد من الإنتاج، يمكن حذفها من اللوح تدريجيًّا.
- **تحقّق بصريّ مطلوب:** افتح لوحة التحكّم في متصفّح حقيقيّ (فاتح + داكن) بعد هذا الترحيل للتأكّد من عدم تعارض.
