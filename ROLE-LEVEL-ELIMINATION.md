# إعدام `role_level` — خطّة اقتلاع العمود من القاعدة المشتركة

> **الحالة: نُفِّذ جوهرها (2026-07-16، مطبَّق حيًّا). العمود مؤجَّل جثّةً بقرار المالك.**
> مبدأ الترقيم **مات**: أُعدم `role_level` من V2 كاملًا، ومن **١٠٣ سياسة RLS**، ومن **٢٦ دالّة**،
> والمصفوفة ضُبطت. تحقّقٌ نهائيّ: صفر سياسة وصفر دالّة تشير إلى العمود.
>
> **باقٍ (مؤجَّل مع دورة حياة V1):** (١) العمود نفسه — يبقى **جثّةً** (لا يقرّر شيئًا؛ يقرؤه V1 فقط)
> حتى يتقاعد V1، ثمّ `DROP COLUMN ... CASCADE` (+فهرس +قيدان). (٢) ٥ دوالّ حافة V1-عصريّة تُفوّض
> بالرقم — تُرحّل وتُنشر مع تقاعد V1. **الإسقاط لا يمنعه إلا قراءات V1 الحيّة (فيزياء لا تصميم).**
>
> `RBAC-MIGRATION.md` المرجع التفصيليّ لمسار التفويض. القياس الأصليّ محفوظ أدناه للسياق.

---

## ٠) المبدأ الحاكم

- **«مَن هو» ≠ «ماذا يستطيع».** هويّة المنصب = `role_name`/`role_id` (سلاسل ثابتة). القدرة = مفتاح
  مُسمّى في `permissions`. **الرقم لا يقرّر سلطةً أبدًا** — لا `if (level >= N)` في أيّ طبقة.
- سابقة صحيحة قائمة: `is_top_admin_role(uuid)` تفحص `role_name IN (...)` لا رقمًا.
- الترتيب البصريّ للهيكل يحتاج نظام فرز — يُحسم في Track B بلا أن يعود بابًا خلفيًّا للترقيم.

---

## ١) سطح الاعتماد الحيّ (قياسٌ على القاعدة الحيّة، 2026-07-16)

| المورد | يعتمد على `role_level` | الإجماليّ |
|---|---|---|
| سياسات RLS | **105** | 249 |
| دوالّ (public) | **26** | 240 |
| دوالّ حافة | **5** | — |
| ملفّات V2 | **8** | — |
| عروض | 0 | — |
| فهارس | 1 | — |

**الدوالّ الحرّاس الرقميّون** (تُعاد على القدرات أو تُزال بعد تحويل مستدعيها):
`current_user_is_admin`، `is_admin_user`، `get_user_max_role_level`، `get_user_highest_role_level`،
`get_user_primary_role`، `gw_is_admin`. ومعها `assign_position` (تسييج `max(role_level) ≥ 8`).

**السياسات** موزّعة على ~46 جدولًا، أثقلها: `membership_cycles`(6)، ثمّ رباعيّات
`activities`/`interview_sessions`/`member_onboarding_tokens`/`membership_*`/`notifications`/`profiles`.

**طبقة التطبيق**: V2 = `lib/auth.ts`، `members/{actions,data}.ts`، `members/credentials/actions.ts`،
`members/structure/{model,orgData}.ts`، `lib/surveys/authz.ts`، `lib/supabase/session.ts`.
الحافة (5) = `migrate-accepted-member`، `create-member-directly`، `resend-onboarding-email`،
`send-member-welcome-email`، `send-position-assignment-email`. **V1 الحيّ**: JS الإدارة يقرأ
`role_level` للرتبة وتسييج الأقسام.

- `vote_weight` **مستقلّ** عن `role_level` — لا يُلمَس. الانتخابات بمقياسها الخاصّ.

---

## ٢) البوّابة الحاكمة — لماذا خطّة لا تنفيذ

العمود **مشترك** بين v2 والموقع V1 الحيّ الذي يخدم الأعضاء الآن. `DROP COLUMN` بينما قارئ حيّ
واحد ما زال يقرؤه = كسرٌ فوريّ لعمليّات النادي. لذا الإسقاط **آخر** خطوة، بعد أن تنتقل كلّ الطبقات.
هذا **تسلسل تبعيّة لمورد مشترك**، لا «تقييد التصميم بـV1»: القرار التصميميّ (لا ترقيم) نُفِّذ في
V2 أوّلًا؛ التوقيت وحده منوطٌ بتوقّف القرّاء.

---

## Track A — التفويض (الأثقل والأخطر)

المرجع التفصيليّ: `RBAC-MIGRATION.md §6-7` (خريطة كلّ عتبة → قدرة مُسمّاة). الملخّص:

1. **إصلاح السباكة القدراتيّة.** `check_permission()` معطّلة (تشير إلى عمود `scope` غير موجود)،
   ودوالّ المنح/السحب تكتب أعمدة/جدول تدقيق غير موجودة. نُثبّت بدائيّة واحدة:
   `has_capability(uid, 'key') → bool`، `SECURITY DEFINER`، مختبَرة، تقرأ `role_permissions`
   (+ تجاوزات `user_specific_permissions`). `get_user_permissions()` تعمل أصلًا ونبني عليها.
2. **ترحيل 105 سياسة + 26 دالّة** من `role_level >= N` إلى `has_capability('key')`. لكلّ سياسة
   اختبارٌ مزدوج: لا تقفل مخوّلًا، ولا تفتح ثغرة. نشر تدريجيّ جدولًا جدولًا.
3. **الحرّاس الرقميّون**: يُعاد تعريفهم على القدرات، ثمّ يُحذفون حين لا مستدعي رقميّ يبقى.

---

## Track B — الترتيب والعرض (بديل الرقم للفرز فقط)

يعتمد `role_level` اليوم في: «أعلى دور» بقائمة الأعضاء (`data.ts`)، وترتيب مقاعد الهيكل/المناصب
(`model.ts`). **قرارٌ مطلوب** بين نهجين:

- **(أ — موصى به) عمود ترتيب صريح** `roles.display_order smallint` (نظير `departments.display_order`
  القائم)، **للفرز والعرض حصرًا، ممنوعٌ من التفويض بحكم التوثيق والمراجعة**. يُبذَر من الترتيب
  الحاليّ. عيبه: يبقى رقمًا — لكنّه رقم عرضٍ صريحُ الغرض، لا رقم سلطة.
- **(ب — بلا أيّ رقم) اشتقاق الترتيب من البنية**: `council_type` ثمّ `membership_kind` ثمّ أعلام
  الرئاسة/القيادة. يمحو الرقم كلّيًّا، لكنّه **لا يفصل الأنداد** (٣ أدوار كانت مستوى ٨) إلا بمِفتاح
  ثانويّ (مثل `vote_weight` أو ترتيب أبجديّ) — فرزٌ أضعف تحديدًا.

`get_user_primary_role` يُعاد على أساس غير رقميّ (هويّة المنصب/القدرة) في كلا النهجين.

---

## Track C — طبقة التطبيق (تتوازى مع A/B لكن تسبق D)

- **V2 — ✅ نُفِّذ (2026-07-16، يمرّ `tsc --noEmit`):** أُعدم `role_level` من ٩ ملفّات.
  - بوّابة اللوحة: `auth.ts` — `getCurrentAdmin` صار يقرأ `get_user_permissions`، و`isAdmin = caps.includes('view_members')`
    (حامِلوها = صفّ `≥8` نفسه، صفر انحراف). أُزيل `ADMIN_MIN` و`roleLevel` كلّيًّا.
  - بوّابات الأفعال بالقدرة: `members/actions.ts` و`credentials/actions.ts` = `manage_member_data`؛
    `structure/actions.ts` (إسناد/إزالة/تحرير وحدة) = `manage_positions`.
  - الترتيب بالاسم: `lib/roleOrder.ts` جديد (قائمة أسماء هرميّة + `roleRank`)؛ `data.ts` («أفضل دور» بالرتبة الاسميّة)،
    `model.ts` (فرز المقاعد/المناصب + حذف حقل `level` الميّت)، `orgData.ts` (أُسقط من الجلب). `surveys/authz.ts` كان قدراتيًّا أصلًا.
  - **تبعة صادقة مقصودة**: `executive_council_president` يملك `view_members` لا `manage_member_data` — يرى الأعضاء ولا يحرّرهم بعد اليوم.
  - **علمٌ للمالك**: `manage_positions` يملكها رئيس النادي وحده اليوم، فالإسناد يقتصر عليه. العلاج قدراتيّ لا رقميّ:
    `INSERT INTO role_permissions` يمنحها لمن يُسنِد فعلًا (HR/التنفيذيّ) — سطرُ بيانات، لا عودة للرقم.
- **الحافة (5)**: عتبات مختلفة لفكرة واحدة اليوم (`< 8` هنا، `< 7` هناك — انجرافٌ مذكور في
  `RBAC-MIGRATION.md §4`). تُوحَّد كلّها بقدرة مُسمّاة.
- **V1 الحيّ (متقاعد)**: JS الإدارة يقرأ `role_level`. بصمة أصغريّة فقط — لا معمار جديد فيه:
  إمّا اشتقاق الرتبة من الاسم/القدرة، أو الاتّكال على أنّ القدرات في RLS تحمي الخادم فعليًّا
  (تسييج العميل زينة لا حارس). الحماية الحقيقيّة انتقلت في Track A.

---

## Track D — الإسقاط + التراجع (آخر خطوة)

1. **تحقّق ما قبل الإسقاط** (بوّابة صفريّة): استعلامٌ يؤكّد **صفر** إشارة إلى `role_level` في
   `pg_policies` و `pg_proc`، وصفر قارئ في الحافة/V2/V1.
2. `ALTER TABLE roles DROP COLUMN role_level;` (+ إسقاط الفهرس والقيد المرتبطين).
3. **التراجع**: العمود يُعاد و`backfill` من `display_order`/الخريطة الثابتة إن لزم — فلا طريق بلا رجعة.

---

## Track E — شاشة الصلاحيات (self-service — تُبنى فور تطبيق ترحيل الفرض)

**قرار المالك (2026-07-16):** تُبنى فور اكتمال ترحيل القاعدة، لتكون كلّ الطبقات دقيقة — وتُنهي
أمرَه اليدويّ المتكرّر «امنح هذا المنصبَ هذه القدرة».

- **الشكل**: صفحة `/dashboard/system/permissions` — مصفوفة (منصب × قدرة) بمربّعات تفعيل تكتب في
  `role_permissions` مباشرة. وطبقةٌ ثانية اختياريّة: تجاوزٌ لشخصٍ بعينه عبر `user_specific_permissions`.
- **شرط الصدق (الفخّ الموثّق في `RBAC-MIGRATION.md §5`)**: تُبنى **بعد** تطبيق migration الفرض (Track A)
  كي تُغيّر النقرةُ الحمايةَ الحقيقيّة في RLS لا القائمة فقط. V2 قدراتيّ أصلًا (Track C) فالشاشة صادقة عليه فورًا،
  وتصير صادقةً على القاعدة كلّها لحظة لحاق الـmigration.
- **المكوّنات**: (١) بدائيّتا كتابة نظيفتان `grant_role_permission`/`revoke_role_permission` = INSERT/DELETE
  على `role_permissions` (الأعمدة الثلاثة الفعليّة)، `SECURITY DEFINER`، مُدقّقتان — البديلتان القديمتان
  معطّلتان (يكتبان `scope`/`granted_by`/جدول تدقيق غير موجودة). (٢) الصفحة: قراءة + تبديل. (٣) بوّابة الشاشة:
  `is_top_admin_role` (رئيس النادي) بدايةً، أو قدرة `manage_permissions` جديدة تُمنَح لمن يشاء المالك.

## التسلسل والمخاطرة

```
A (السباكة → الحرّاس → 105 سياسة) ──┐
B (عمود/اشتقاق الترتيب) ───────────┼──> C (V2 · الحافة · V1) ──> D (تحقّق صفريّ → DROP)
                                   ┘
```

migrations مُراجَعة، اختبار كلّ سياسة، نشر تدريجيّ، فترة مراقبة قبل D. **مشروع أيّام** بمخاطرة
إنتاج حقيقيّة لأنّ V1 حيّ على القاعدة نفسها — لا مهمّة عابرة.
