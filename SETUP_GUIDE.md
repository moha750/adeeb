# دليل إعداد نظام إدارة نادي أدِيب

هذا الدليل يشرح خطوة بخطوة كيفية إعداد النظام الجديد من الصفر.

## 📋 المتطلبات الأساسية

- حساب Supabase (مجاني أو مدفوع)
- متصفح حديث
- الوصول إلى لوحة تحكم Supabase

## 🚀 خطوات الإعداد

### الخطوة 1: إعداد قاعدة البيانات

#### 1.1 تسجيل الدخول إلى Supabase

1. اذهب إلى [https://supabase.com](https://supabase.com)
2. سجل الدخول إلى حسابك
3. اختر المشروع الخاص بنادي أدِيب

#### 1.2 تنفيذ Migration الأول (إنشاء الجداول)

1. من القائمة الجانبية، اختر **SQL Editor**
2. انقر على **New query**
3. افتح الملف `supabase/migrations/001_initial_schema.sql`
4. انسخ المحتوى بالكامل والصقه في SQL Editor
5. انقر على **Run** أو اضغط `Ctrl + Enter`
6. انتظر حتى تظهر رسالة "Success"

**ملاحظة**: هذه الخطوة تنشئ:
- 17 جدول رئيسي
- الفهارس (Indexes)
- الدوال المساعدة (Functions)
- طرق العرض (Views)

#### 1.3 تنفيذ Migration الثاني (سياسات الأمان)

1. في SQL Editor، انقر على **New query**
2. افتح الملف `supabase/migrations/002_rls_policies.sql`
3. انسخ المحتوى بالكامل والصقه
4. انقر على **Run**
5. انتظر حتى تظهر رسالة "Success"

**ملاحظة**: هذه الخطوة تفعل:
- Row Level Security على جميع الجداول
- سياسات الوصول بناءً على الأدوار
- حماية البيانات

#### 1.4 تنفيذ Migration الثالث (البيانات الأولية)

1. في SQL Editor، انقر على **New query**
2. افتح الملف `supabase/migrations/003_seed_data.sql`
3. انسخ المحتوى بالكامل والصقه
4. انقر على **Run**
5. انتظر حتى تظهر رسالة "Success"

**ملاحظة**: هذه الخطوة تضيف:
- 7 لجان
- 10 أدوار وظيفية
- 25+ صلاحية
- ربط الأدوار بالصلاحيات

### الخطوة 2: إنشاء أول مستخدم (رئيس النادي)

#### 2.1 إنشاء المستخدم في Authentication

1. من القائمة الجانبية، اختر **Authentication**
2. اختر **Users**
3. انقر على **Add user** → **Create new user**
4. أدخل البيانات:
   - **Email**: admin@adeeb.club (أو أي بريد تريده)
   - **Password**: كلمة مرور قوية (على الأقل 8 أحرف)
   - **Auto Confirm User**: ✅ فعّل هذا الخيار
5. انقر على **Create user**
6. **مهم جداً**: انسخ الـ **UUID** الخاص بالمستخدم (ستحتاجه في الخطوة التالية)

#### 2.2 إضافة الملف الشخصي والدور

1. ارجع إلى **SQL Editor**
2. انقر على **New query**
3. الصق الكود التالي واستبدل `YOUR_USER_UUID` بالـ UUID الذي نسخته:

```sql
-- إضافة الملف الشخصي
INSERT INTO public.profiles (id, username, full_name, email, account_status)
VALUES (
    'YOUR_USER_UUID',  -- استبدل هذا بالـ UUID الحقيقي
    'admin',
    'مدير النظام',
    'admin@adeeb.club',  -- استبدل هذا بالبريد الذي استخدمته
    'active'
);

-- تعيين دور رئيس النادي
INSERT INTO public.user_roles (user_id, role_id, is_active, assigned_at)
SELECT 
    'YOUR_USER_UUID',  -- استبدل هذا بالـ UUID الحقيقي
    id,
    true,
    NOW()
FROM public.roles
WHERE role_name = 'club_president';
```

4. انقر على **Run**
5. تأكد من ظهور رسالة "Success"

### الخطوة 3: التحقق من الإعداد

#### 3.1 التحقق من الجداول

1. من القائمة الجانبية، اختر **Table Editor**
2. يجب أن ترى جميع الجداول التالية:
   - profiles
   - committees
   - roles
   - user_roles
   - permissions
   - role_permissions
   - projects
   - tasks
   - meetings
   - attendance
   - notifications
   - activity_log
   - reports
   - task_comments
   - task_attachments
   - member_evaluations
   - announcements

#### 3.2 التحقق من البيانات الأولية

1. في **Table Editor**, افتح جدول **committees**
2. يجب أن ترى 7 لجان

3. افتح جدول **roles**
4. يجب أن ترى 10 أدوار

5. افتح جدول **profiles**
6. يجب أن ترى المستخدم الذي أنشأته

7. افتح جدول **user_roles**
8. يجب أن ترى دور رئيس النادي مسند للمستخدم

### الخطوة 4: اختبار تسجيل الدخول

1. افتح الملف `admin/login.html` في المتصفح
2. أدخل البريد الإلكتروني وكلمة المرور
3. انقر على **تسجيل الدخول**
4. يجب أن يتم توجيهك إلى لوحة التحكم

**إذا نجح تسجيل الدخول**: تهانينا! النظام جاهز للاستخدام 🎉

**إذا فشل تسجيل الدخول**: راجع قسم حل المشاكل أدناه

## 🔧 إضافة مستخدمين جدد

بعد إعداد النظام، يمكنك إضافة مستخدمين جدد بطريقتين:

### الطريقة 1: من لوحة التحكم (موصى بها)

1. سجل الدخول كرئيس النادي
2. اذهب إلى **إدارة المستخدمين**
3. انقر على **إضافة مستخدم**
4. أدخل البيانات واختر الدور
5. سيتم إنشاء المستخدم تلقائياً

### الطريقة 2: يدوياً من Supabase

كرر الخطوة 2 أعلاه، لكن استخدم أدوار مختلفة:

```sql
-- مثال: إضافة قائد لجنة الفعاليات

-- 1. أنشئ المستخدم في Authentication أولاً
-- 2. انسخ الـ UUID
-- 3. نفذ هذا الكود:

INSERT INTO public.profiles (id, username, full_name, email, account_status)
VALUES (
    'USER_UUID_HERE',
    'events_leader',
    'قائد لجنة الفعاليات',
    'events@adeeb.club',
    'active'
);

-- تعيين دور قائد لجنة
INSERT INTO public.user_roles (user_id, role_id, committee_id, is_active)
SELECT 
    'USER_UUID_HERE',
    r.id,
    c.id,
    true
FROM public.roles r
CROSS JOIN public.committees c
WHERE r.role_name = 'committee_leader'
AND c.committee_name_ar = 'لجنة الفعاليات';
```

## 📊 هيكل الأدوار المتاحة

| اسم الدور في الكود | الاسم بالعربية | المستوى | يحتاج لجنة؟ |
|-------------------|----------------|---------|-------------|
| `club_president` | رئيس نادي أدِيب | 10 | لا |
| `hr_committee_leader` | قائد لجنة الموارد البشرية | 9 | لا |
| `qa_committee_leader` | قائد لجنة الضمان والجودة | 9 | لا |
| `hr_admin_member` | عضو إداري - الموارد البشرية | 8 | نعم |
| `qa_admin_member` | عضو إداري - الضمان والجودة | 8 | نعم |
| `administrative_council_president` | رئيس المجلس الإداري | 8 | لا |
| `committee_leader` | قائد لجنة | 7 | نعم |
| `deputy_committee_leader` | نائب قائد لجنة | 6 | نعم |
| `committee_member` | عضو لجنة | 5 | نعم |
| `regular_member` | عضو عادي | 1 | لا |

## 🏢 اللجان المتاحة

| اسم اللجنة في الكود | الاسم بالعربية |
|---------------------|----------------|
| `events` | لجنة الفعاليات |
| `storytellers` | لجنة الرواة |
| `writing` | لجنة التأليف |
| `ambassadors` | لجنة السُفراء |
| `production` | لجنة الإنتاج |
| `design` | لجنة التصميم |
| `marketing` | لجنة التسويق |

## ❗ حل المشاكل الشائعة

### المشكلة: خطأ في تنفيذ SQL

**الحل**:
1. تأكد من تنفيذ الملفات بالترتيب الصحيح
2. تحقق من عدم وجود أخطاء إملائية
3. تأكد من أن الجداول لم تُنشأ مسبقاً
4. إذا لزم الأمر، احذف الجداول القديمة وابدأ من جديد

### المشكلة: لا أستطيع تسجيل الدخول

**الحل**:
1. تحقق من صحة البريد الإلكتروني وكلمة المرور
2. تأكد من تفعيل المستخدم في Authentication
3. تحقق من وجود الملف الشخصي في جدول `profiles`
4. تأكد من تعيين الدور في جدول `user_roles`
5. افتح Console في المتصفح (F12) وابحث عن رسائل الخطأ

### المشكلة: "حسابك غير مسجل في النظام"

**الحل**:
- لم يتم إضافة الملف الشخصي في جدول `profiles`
- نفذ الخطوة 2.2 مرة أخرى

### المشكلة: "ليس لديك صلاحية الوصول"

**الحل**:
- لم يتم تعيين دور للمستخدم
- أو الدور المعين أقل من المستوى 5
- تحقق من جدول `user_roles`

### المشكلة: الصفحة فارغة أو لا تعمل

**الحل**:
1. تحقق من إعدادات Supabase في `supabase-config.js`
2. تأكد من صحة `SUPABASE_URL` و `SUPABASE_ANON_KEY`
3. افتح Console في المتصفح وابحث عن الأخطاء
4. تأكد من تحميل جميع المكتبات بشكل صحيح

## 🔄 إعادة تعيين النظام

إذا أردت البدء من جديد:

```sql
-- احذف جميع الجداول (احذر! هذا سيحذف جميع البيانات)
DROP TABLE IF EXISTS public.announcements CASCADE;
DROP TABLE IF EXISTS public.member_evaluations CASCADE;
DROP TABLE IF EXISTS public.task_attachments CASCADE;
DROP TABLE IF EXISTS public.task_comments CASCADE;
DROP TABLE IF EXISTS public.reports CASCADE;
DROP TABLE IF EXISTS public.activity_log CASCADE;
DROP TABLE IF EXISTS public.notifications CASCADE;
DROP TABLE IF EXISTS public.attendance CASCADE;
DROP TABLE IF EXISTS public.meetings CASCADE;
DROP TABLE IF EXISTS public.tasks CASCADE;
DROP TABLE IF EXISTS public.projects CASCADE;
DROP TABLE IF EXISTS public.role_permissions CASCADE;
DROP TABLE IF EXISTS public.permissions CASCADE;
DROP TABLE IF EXISTS public.user_roles CASCADE;
DROP TABLE IF EXISTS public.roles CASCADE;
DROP TABLE IF EXISTS public.committees CASCADE;
DROP TABLE IF EXISTS public.profiles CASCADE;

-- احذف الدوال
DROP FUNCTION IF EXISTS update_updated_at_column CASCADE;
DROP FUNCTION IF EXISTS get_user_permissions CASCADE;
DROP FUNCTION IF EXISTS get_user_highest_role_level CASCADE;
DROP FUNCTION IF EXISTS check_user_permission CASCADE;
DROP FUNCTION IF EXISTS log_activity CASCADE;

-- احذف Views
DROP VIEW IF EXISTS user_roles_view CASCADE;
DROP VIEW IF EXISTS tasks_detailed_view CASCADE;
DROP VIEW IF EXISTS projects_detailed_view CASCADE;
```

ثم ابدأ من الخطوة 1 مرة أخرى.

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع هذا الدليل بعناية
2. تحقق من Console في المتصفح
3. راجع سجلات Supabase
4. تواصل مع فريق الدعم

---

**ملاحظة مهمة**: احتفظ بنسخة احتياطية من بيانات Supabase بشكل دوري!

تم إعداد هذا الدليل بواسطة فريق تطوير نادي أدِيب
الإصدار 1.0 - يناير 2026
