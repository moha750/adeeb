# تدقيق شامل لقاعدة بيانات نادي أديب + تقييم نظام الصلاحيات (RBAC)

> تدقيق بأدوات Supabase الرسمية (list_tables, advisors أمن/أداء, فحص pg_policies و pg_proc).
> التاريخ: 2026-07-05. المشروع: `nnlhkfeybyhvlinbqqfa`.

---

## 0) الخلاصة التنفيذية — الأهم

**قاعدة بياناتك ناضجة ومعمارها جيّد. لا تُعِد بناءها.** 

بدأنا هذا التدقيق لأنك ظننتَ أن نظام الأدوار «مرقّم» بشكل هشّ. الحقيقة بعد الفحص: **نظام صلاحياتك متقدّم وقائم على القدرات (capability-based RBAC) فعلًا** — بجدول صلاحيات مُسمّاة، وربط أدوار-صلاحيات، وصلاحيات فردية لكل مستخدم، ونطاقات (إدارة/لجنة). مشكلة «أهل الدفّة» لم تكن خللًا في تصميمك، بل **فلتر ناقص في استعلامي أنا** (`is_active`).

> **توصيتي الصريحة كخبير:** إعادة بناء قاعدة تضم ~90 جدولًا وأنظمة كاملة (عضوية، انتخابات، استبيانات، أخبار…) = أشهر من العمل والمخاطرة **لإعادة اختراع ما يعمل أصلًا**. الصواب: **تقوية وتحسين موضعيّان** (أمن + أداء)، وهي منخفضة المخاطر وتُنفّذ تدريجيًا بلا انقطاع.

**ما تحتاجه فعلًا (بالأولوية):**
1. 🟠 تحسين أداء RLS (٣٧٥ ملاحظة) + فهارس مفاتيح أجنبية ناقصة (٤٥).
2. 🟠 تقوية أمنية للدوال (search_path) ومراجعة Views بصلاحية المُنشئ (٢).
3. 🟢 لمسة اختيارية على الأدوار (علم صريح للمجلس) — تحسين عرض، لا إصلاح.

---

## 1) نظرة عامة على البنية

**~90 جدولًا، كلها RLS مُفعّل.** الأنظمة الرئيسية:

| النظام | جداول أساسية | حجم |
|---|---|---|
| **الأدوار والصلاحيات** | `roles`(12) · `permissions`(29) · `role_permissions`(146) · `user_roles`(245) · `user_specific_permissions` | متقدّم |
| **البنية التنظيمية** | `departments`(4) · `councils`(2) · `committees`(11) | — |
| **الأعضاء** | `profiles`(215) · `member_details`(157) · `member_onboarding_tokens`(125) | — |
| **مسار العضوية** | `membership_applications`(58) · `membership_interviews`(53) · `interview_sessions/slots`(15/119) · `membership_accepted_members`(32) + أرشيفات | ناضج |
| **الاستبيانات** | `surveys`(20) · `survey_questions`(79) · `survey_responses`(461) · `survey_answers`(1074) | ناضج |
| **الأخبار** | `news`(15) + سير عمل الكتّاب (`news_writer_assignments`, `news_field_permissions`, `news_activity_log`…) | متقدّم |
| **الأنشطة** | `activities`(4) · `activity_reservations`(183) · `visitors`(147) | — |
| **الانتخابات** | `elections`(8) · `election_candidates`(13) · `election_votes`(9) · `election_vote_weights`(9) · `election_audit_log`(73) | متقدّم |
| **التفاعل/الإشعارات** | `notifications`(64) · `news_likes`(55) · `comment_likes` · … | — |
| **التحليلات** | `site_pageviews`(3111) · `site_visitors`(1129) · `file_downloads`(1443) | — |
| **ألعاب** | `guess_word_*` | — |

**التقييم:** بنية غنيّة ومنظّمة، بأرشفة وسجلّات تدقيق ومسارات عمل — عمل جادّ لا يُستهان به.

---

## 2) تقييم نظام الأدوار/الصلاحيات (سبب هذا التدقيق)

**النتيجة: نظام محترم وقائم على القدرات — احتفظ به.**

- **صلاحيات مُسمّاة صريحة:** `check_permission(user, permission_key, scope, context)` — أي التحقّق يكون على **قدرة باسمها** (`activities.manage`…)، لا على رقم. ويوجد `check_any_permission`, `get_user_all_permissions`, `get_user_permissions_by_module`.
- **ربط أدوار-صلاحيات:** جدول `role_permissions` (146 ربطًا) + دوال `grant_permission_to_role` / `revoke_permission_from_role`.
- **نطاقات (Scopes):** الصلاحية لها نطاق (`all`/`department`/`committee`) وسياق — وهذا **تصميم متقدّم**.
- **تجاوزات لكل مستخدم:** `user_specific_permissions` + `grant_user_specific_permission` (منح/منع فردي مؤقّت).
- **`role_level`:** مساعد هرمي **ثانوي** للعرض والترتيب وبعض الاختصارات (`is_top_admin_role`, `get_user_max_role_level`) — ليس أساس الصلاحية.

**إذًا:** ازدواج «قائد/منسّق = 6» **لا يؤثّر على الصلاحيات** (لأنها بالاسم عبر `role_permissions`)، بل يؤثّر فقط على تجميع بصري بالرقم. ومشكلة المجلس كانت استعلامي (تجاهل `is_active`) — **صُحّحت**.

**لمسة اختيارية (تحسين لا إصلاح):** إضافة علم صريح `is_council`/تصنيف على `roles` لتعريف «أهل الدفّة» بالاسم لا بعتبة رقمية. صغيرة وآمنة.

---

## 3) نتائج الأمن (مرتّبة بصدق، بلا تهويل)

المدقّق أطلق **392 ملاحظة أمنية (خطآن + 390 تحذيرًا)** — لكن أغلبها **مقصود أو تقوية**، لا ثغرات:

| # | البند | العدد | التقييم الحقيقي |
|---|---|---|---|
| ✅ | **Views بصلاحية المُنشئ (SECURITY DEFINER)** | ~~2~~ 0 | **سُدّد 2026-07-16:** `surveys_overview`, `survey_responses_detailed` أُسقطتا في `20260716_survey_v2_prune.sql` — صفر قارئ لهما وكانتا تتجاوزان RLS وتكشفان بريد المنشئ وIP المجيب. |
| 🟠 | **دوال بلا `search_path` ثابت** | 59 | تقوية قياسية ضد اختطاف search_path — يُضاف `set search_path=public`. |
| 🟠 | **دوال SECURITY DEFINER قابلة للتنفيذ عامًّا/للمسجّلين** | 146 / 168 | معظمها مقصود (مساعِدات صلاحيات، RPC عامة مثل `track-pageview`, `get_board_members`) — **مراجعة أيّها يجب تقييده**. |
| 🟡 | **RLS «دائمًا صحيح»** | 14 | **تحقّقتُ:** معظمها سياسات **إدخال عام مقصودة** (تقديم طلب/تعليق/إعجاب). `membership_applications` = إدخال فقط (لا تعرّض قراءة). يبقى مراجعة `file_downloads` (قراءة واسعة للمسجّلين). |
| 🟡 | **Buckets عامة تسمح بالسرد** | 2 | `avatars`, `images` — أصول عامة أصلًا؛ أثر منخفض. |
| 🟡 | **حماية كلمات المرور المسرّبة معطّلة** | 1 | تفعيل بنقرة في إعدادات Auth. |

---

## 4) نتائج الأداء (القيمة العملية الأكبر)

**467 ملاحظة** — هذه **إصلاحات حقيقية ومجدية**، خصوصًا مع نمو البيانات:

| # | البند | العدد | الأثر / الحل |
|---|---|---|---|
| 🟠 | **إعادة تقييم `auth.uid()` لكل صف في RLS** | 195 | بطء عند الحجم — الحل: لفّها `(select auth.uid())` فتُقيَّم مرة واحدة. |
| 🟠 | **سياسات RLS متعدّدة متساهلة** | 180 | كل سياسة تُقيَّم على حدة — **دمجها** يسرّع القراءة. |
| 🟠 | **مفاتيح أجنبية بلا فهرس** | 45 | بطء الوصلات/الحذف (انتخابات، عضوية، حجوزات…) — إضافة فهارس. |
| 🟢 | **فهارس غير مستخدمة** | 47 | إهدار مساحة/كتابة — حذفها بأمان. |

---

## 5) التوصية والخطة (تقوية، لا هدم)

**كلها هجرات (migrations) آمنة تدريجية، بلا انقطاع، ولا تمسّ بناء V2 الأمامي.**

- **P0 — تعرّض/سلوك خاطئ:** مراجعة الـ2 Views (SECURITY DEFINER)، ومراجعة `file_downloads` SELECT.
- **P1 — تقوية الدوال:** إضافة `search_path` للـ59 دالة، ومراجعة صلاحيات تنفيذ الدوال العامة.
- **P2 — أداء:** لفّ `auth.uid()` (195)، دمج السياسات المتساهلة (180)، إضافة فهارس FK (45)، حذف الفهارس غير المستخدمة (47).
- **P3 — لمسات RBAC:** علم `is_council` صريح + تنظيف بسيط.
- **إعدادات:** تفعيل حماية كلمات المرور المسرّبة.

> **قرار المجلس (منجز):** دالة `get_board_members()` تعتمد الآن `is_active` فقط — تعرض المناصب الحالية الصحيحة.

---

## 6) الحكم النهائي

قاعدتك ليست المشكلة — بل أحد أفضل أجزاء مشروعك. ما بدأ كشكٍّ في «الترقيم» انتهى إلى اكتشاف نظام صلاحيات **أنضج مما توقّعنا**. الطريق ليس إعادة البناء، بل **جولة تقوية وأداء مركّزة** نُنفّذها بأمان متى شئت.
