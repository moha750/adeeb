# دليل تعديل جلسات المقابلات وتحديث الروابط

## 📋 نظرة عامة

تم إضافة ميزتين جديدتين إلى نظام إدارة جلسات المقابلات:

### **1️⃣ تعديل الجلسة**
✅ إمكانية تعديل جميع بيانات الجلسة (الاسم، التاريخ، الوقت، النوع، إلخ)  
✅ تحديث رابط المقابلة أو الموقع حسب نوع الجلسة  
✅ تغيير مدة المقابلة وأوقات البداية والنهاية

### **2️⃣ تحديث رابط المقابلة**
✅ زر مخصص لتحديث رابط المقابلة الأونلاين بسرعة  
✅ مفيد عند انتهاء صلاحية روابط Zoom أو Google Meet  
✅ التحقق من صحة الرابط قبل الحفظ

---

## 🎯 حالات الاستخدام

### **السيناريو 1: انتهاء صلاحية رابط Zoom**

```
المشكلة:
- رابط Zoom ينتهي بعد 30 يوم
- لديك جلسة مقابلات بعد أسبوع
- تحتاج لتحديث الرابط فقط دون تعديل باقي البيانات

الحل:
1. اضغط على زر "تحديث رابط المقابلة" 🎥
2. أدخل الرابط الجديد
3. احفظ ✅

النتيجة:
- الرابط محدث
- جميع الحجوزات تستخدم الرابط الجديد
- لم تتأثر باقي بيانات الجلسة
```

---

### **السيناريو 2: تغيير موعد الجلسة**

```
المشكلة:
- تم تأجيل الجلسة ليوم آخر
- تحتاج لتحديث التاريخ والوقت

الحل:
1. اضغط على زر "تعديل الجلسة" ✏️
2. غيّر التاريخ والوقت
3. احفظ التعديلات ✅

النتيجة:
- التاريخ والوقت محدثان
- الفترات الزمنية تُعاد إنشاؤها تلقائياً
- المتقدمون يرون الموعد الجديد
```

---

### **السيناريو 3: تغيير نوع المقابلة**

```
المشكلة:
- كانت المقابلة حضورية
- تم تحويلها لأونلاين بسبب ظروف طارئة

الحل:
1. اضغط على زر "تعديل الجلسة" ✏️
2. غيّر النوع من "حضوري" إلى "أونلاين"
3. أدخل رابط المقابلة
4. احفظ التعديلات ✅

النتيجة:
- نوع المقابلة محدث
- رابط المقابلة متاح للمتقدمين
- الموقع الحضوري تم إزالته
```

---

## 🖥️ واجهة المستخدم

### **جدول الجلسات - الأزرار الجديدة**

```
┌─────────────────────────────────────────────────────────┐
│ الإجراءات                                              │
├─────────────────────────────────────────────────────────┤
│ 👁️ عرض التفاصيل                                       │
│ ✏️ تعديل الجلسة          ← جديد!                      │
│ 🎥 تحديث رابط المقابلة   ← جديد!                      │
│ 🔗 نسخ الرابط                                          │
│ ⏸️ تعطيل/تفعيل                                         │
│ 🗑️ حذف                                                 │
└─────────────────────────────────────────────────────────┘
```

---

## 🔧 الميزات التفصيلية

### **1. زر تعديل الجلسة**

**الموقع:** في عمود "الإجراءات" لكل جلسة  
**الأيقونة:** `fa-pen-to-square` (قلم)  
**الوظيفة:** فتح نافذة منبثقة لتعديل جميع بيانات الجلسة

**الحقول القابلة للتعديل:**
- ✅ اسم الجلسة
- ✅ الوصف
- ✅ تاريخ الجلسة
- ✅ وقت البداية
- ✅ وقت النهاية
- ✅ مدة كل مقابلة (بالدقائق)
- ✅ نوع المقابلة (أونلاين/حضوري/هاتفي)
- ✅ رابط الاجتماع (للأونلاين)
- ✅ الموقع (للحضوري)

**التحقق من البيانات:**
```javascript
// التحقق من الحقول المطلوبة
if (!name || !date || !startTime || !endTime || !duration) {
    Swal.showValidationMessage('يرجى ملء جميع الحقول المطلوبة');
    return false;
}

// التحقق من منطقية الأوقات
if (startTime >= endTime) {
    Swal.showValidationMessage('وقت النهاية يجب أن يكون بعد وقت البداية');
    return false;
}
```

**التفاعل الديناميكي:**
```javascript
// عند تغيير نوع المقابلة
typeSelect.addEventListener('change', () => {
    if (typeSelect.value === 'online') {
        // إظهار حقل رابط الاجتماع
        linkGroup.style.display = 'block';
        locationGroup.style.display = 'none';
    } else if (typeSelect.value === 'in_person') {
        // إظهار حقل الموقع
        linkGroup.style.display = 'none';
        locationGroup.style.display = 'block';
    } else {
        // إخفاء كلاهما (للهاتفي)
        linkGroup.style.display = 'none';
        locationGroup.style.display = 'none';
    }
});
```

---

### **2. زر تحديث رابط المقابلة**

**الموقع:** في عمود "الإجراءات" لكل جلسة  
**الأيقونة:** `fa-video` (كاميرا فيديو)  
**الوظيفة:** تحديث رابط المقابلة الأونلاين بسرعة

**التحقق من نوع الجلسة:**
```javascript
if (session.interview_type !== 'online') {
    showNotification('هذه الميزة متاحة فقط للمقابلات الأونلاين', 'warning');
    return;
}
```

**عرض الرابط الحالي:**
```html
<div class="form-group">
    <label>الرابط الحالي:</label>
    <input type="text" value="${session.meeting_link || 'لا يوجد'}" 
           readonly style="background: #f1f5f9;">
</div>
```

**التحقق من صحة الرابط:**
```javascript
try {
    new URL(link);
} catch (e) {
    Swal.showValidationMessage('الرابط غير صحيح. يرجى إدخال رابط صحيح');
    return false;
}
```

**نصائح للمستخدم:**
```html
<p style="font-size: 0.85rem; color: #64748b;">
    <i class="fa-solid fa-lightbulb"></i>
    نصيحة: تأكد من صحة الرابط قبل الحفظ
</p>
```

---

## 💻 الكود المُضاف

### **في `interview-sessions-manager.js`**

#### **1. تحديث جدول الجلسات (renderSessions)**

```javascript
<button class="icon-btn" onclick="window.interviewSessionsManager.editSession('${session.id}')" title="تعديل الجلسة">
    <i class="fa-solid fa-pen-to-square"></i>
</button>
<button class="icon-btn" onclick="window.interviewSessionsManager.updateMeetingLink('${session.id}')" title="تحديث رابط المقابلة">
    <i class="fa-solid fa-video"></i>
</button>
```

---

#### **2. دالة editSession**

```javascript
async function editSession(sessionId) {
    try {
        const session = allSessions.find(s => s.id === sessionId);
        if (!session) return;

        const { value: formValues } = await Swal.fire({
            title: 'تعديل جلسة المقابلات',
            html: `
                <!-- نموذج تعديل كامل -->
            `,
            width: '600px',
            showCancelButton: true,
            confirmButtonText: 'حفظ التعديلات',
            cancelButtonText: 'إلغاء',
            didOpen: () => {
                // ربط الأحداث الديناميكية
            },
            preConfirm: () => {
                // التحقق من البيانات
            }
        });

        if (!formValues) return;

        const { error } = await window.sbClient
            .from('interview_sessions')
            .update({
                session_name: formValues.name,
                session_description: formValues.description || null,
                session_date: formValues.date,
                start_time: formValues.startTime,
                end_time: formValues.endTime,
                slot_duration: parseInt(formValues.duration),
                interview_type: formValues.type,
                meeting_link: formValues.type === 'online' ? formValues.link : null,
                location: formValues.type === 'in_person' ? formValues.location : null
            })
            .eq('id', sessionId);

        if (error) throw error;

        showNotification('تم تحديث الجلسة بنجاح', 'success');
        await loadSessions();

    } catch (error) {
        console.error('خطأ في تعديل الجلسة:', error);
        showNotification('خطأ في تحديث الجلسة', 'error');
    }
}
```

---

#### **3. دالة updateMeetingLink**

```javascript
async function updateMeetingLink(sessionId) {
    try {
        const session = allSessions.find(s => s.id === sessionId);
        if (!session) return;

        // التحقق من نوع الجلسة
        if (session.interview_type !== 'online') {
            showNotification('هذه الميزة متاحة فقط للمقابلات الأونلاين', 'warning');
            return;
        }

        const { value: newLink } = await Swal.fire({
            title: 'تحديث رابط المقابلة',
            html: `
                <div style="text-align: right;">
                    <p style="margin-bottom: 1rem; color: #64748b;">
                        <i class="fa-solid fa-info-circle"></i>
                        قم بتحديث رابط المقابلة الأونلاين (Zoom, Google Meet, Teams, إلخ)
                    </p>
                    <div class="form-group">
                        <label>الرابط الحالي:</label>
                        <input type="text" class="swal2-input" value="${session.meeting_link || 'لا يوجد'}" readonly style="width: 100%; background: #f1f5f9;">
                    </div>
                    <div class="form-group">
                        <label>الرابط الجديد: *</label>
                        <input type="url" id="swal-new-link" class="swal2-input" placeholder="https://meet.google.com/xxx-yyyy-zzz" style="width: 100%;">
                    </div>
                    <p style="margin-top: 1rem; font-size: 0.85rem; color: #64748b;">
                        <i class="fa-solid fa-lightbulb"></i>
                        نصيحة: تأكد من صحة الرابط قبل الحفظ
                    </p>
                </div>
            `,
            width: '500px',
            showCancelButton: true,
            confirmButtonText: 'تحديث الرابط',
            cancelButtonText: 'إلغاء',
            preConfirm: () => {
                const link = document.getElementById('swal-new-link').value;

                if (!link) {
                    Swal.showValidationMessage('يرجى إدخال رابط المقابلة');
                    return false;
                }

                // التحقق من صحة الرابط
                try {
                    new URL(link);
                } catch (e) {
                    Swal.showValidationMessage('الرابط غير صحيح. يرجى إدخال رابط صحيح');
                    return false;
                }

                return link;
            }
        });

        if (!newLink) return;

        const { error } = await window.sbClient
            .from('interview_sessions')
            .update({ meeting_link: newLink })
            .eq('id', sessionId);

        if (error) throw error;

        showNotification('تم تحديث رابط المقابلة بنجاح', 'success');
        await loadSessions();

    } catch (error) {
        console.error('خطأ في تحديث رابط المقابلة:', error);
        showNotification('خطأ في تحديث الرابط', 'error');
    }
}
```

---

#### **4. تصدير الدوال**

```javascript
window.interviewSessionsManager = {
    init: initInterviewSessionsManager,
    createNewSession,
    viewSession,
    editSession,           // ← جديد
    updateMeetingLink,     // ← جديد
    copyLink,
    toggleSession,
    deleteSession
};
```

---

## 🧪 الاختبار

### **اختبار 1: تعديل جلسة موجودة**

```
1. افتح لوحة الإدارة → جلسات المقابلات ✅
2. اضغط على زر "تعديل الجلسة" ✏️ لأي جلسة ✅
3. غيّر اسم الجلسة ✅
4. غيّر التاريخ ✅
5. غيّر الوقت ✅
6. اضغط "حفظ التعديلات" ✅
7. تحقق من تحديث البيانات في الجدول ✅
```

---

### **اختبار 2: تحديث رابط مقابلة أونلاين**

```
1. افتح لوحة الإدارة → جلسات المقابلات ✅
2. اختر جلسة نوعها "أونلاين" ✅
3. اضغط على زر "تحديث رابط المقابلة" 🎥 ✅
4. أدخل رابط جديد (مثل: https://meet.google.com/abc-def-ghi) ✅
5. اضغط "تحديث الرابط" ✅
6. تحقق من تحديث الرابط ✅
7. افتح صفحة الحجز وتحقق من ظهور الرابط الجديد ✅
```

---

### **اختبار 3: محاولة تحديث رابط لجلسة حضورية**

```
1. افتح لوحة الإدارة → جلسات المقابلات ✅
2. اختر جلسة نوعها "حضوري" ✅
3. اضغط على زر "تحديث رابط المقابلة" 🎥 ✅
4. ✅ يجب أن تظهر رسالة: "هذه الميزة متاحة فقط للمقابلات الأونلاين"
5. ✅ لا يتم فتح نافذة التحديث
```

---

### **اختبار 4: تغيير نوع الجلسة من حضوري لأونلاين**

```
1. افتح لوحة الإدارة → جلسات المقابلات ✅
2. اضغط على زر "تعديل الجلسة" لجلسة حضورية ✅
3. غيّر النوع من "حضوري" إلى "أونلاين" ✅
4. ✅ يجب أن يظهر حقل "رابط الاجتماع"
5. ✅ يجب أن يختفي حقل "الموقع"
6. أدخل رابط المقابلة ✅
7. احفظ التعديلات ✅
8. تحقق من تحديث النوع والرابط ✅
```

---

### **اختبار 5: التحقق من صحة الرابط**

```
1. اضغط على زر "تحديث رابط المقابلة" ✅
2. أدخل رابط غير صحيح (مثل: "abc123") ✅
3. اضغط "تحديث الرابط" ✅
4. ✅ يجب أن تظهر رسالة خطأ: "الرابط غير صحيح"
5. أدخل رابط صحيح ✅
6. ✅ يتم الحفظ بنجاح
```

---

## 📊 ملخص التغييرات

| المكون | التغيير | الحالة |
|--------|---------|--------|
| **الواجهة** |
| جدول الجلسات | إضافة زر "تعديل الجلسة" | ✅ |
| جدول الجلسات | إضافة زر "تحديث رابط المقابلة" | ✅ |
| **الوظائف** |
| `editSession()` | دالة جديدة لتعديل جميع بيانات الجلسة | ✅ |
| `updateMeetingLink()` | دالة جديدة لتحديث رابط المقابلة فقط | ✅ |
| **التصدير** |
| `window.interviewSessionsManager` | إضافة `editSession` و `updateMeetingLink` | ✅ |

---

## 🔒 الأمان والتحقق

### **1. التحقق من نوع الجلسة**

```javascript
if (session.interview_type !== 'online') {
    showNotification('هذه الميزة متاحة فقط للمقابلات الأونلاين', 'warning');
    return;
}
```

**الفائدة:** منع تحديث رابط المقابلة للجلسات الحضورية أو الهاتفية.

---

### **2. التحقق من صحة الرابط**

```javascript
try {
    new URL(link);
} catch (e) {
    Swal.showValidationMessage('الرابط غير صحيح');
    return false;
}
```

**الفائدة:** التأكد من أن الرابط المُدخل صحيح قبل الحفظ.

---

### **3. التحقق من الحقول المطلوبة**

```javascript
if (!name || !date || !startTime || !endTime || !duration) {
    Swal.showValidationMessage('يرجى ملء جميع الحقول المطلوبة');
    return false;
}
```

**الفائدة:** منع حفظ بيانات ناقصة.

---

### **4. التحقق من منطقية الأوقات**

```javascript
if (startTime >= endTime) {
    Swal.showValidationMessage('وقت النهاية يجب أن يكون بعد وقت البداية');
    return false;
}
```

**الفائدة:** منع إنشاء جلسات بأوقات غير منطقية.

---

## 💡 نصائح للاستخدام

### **1. تحديث روابط Zoom بانتظام**

```
⚠️ روابط Zoom تنتهي بعد 30 يوم
✅ استخدم زر "تحديث رابط المقابلة" قبل الجلسة بيوم
✅ تحقق من الرابط قبل إرساله للمتقدمين
```

---

### **2. تغيير نوع الجلسة**

```
إذا احتجت لتغيير نوع الجلسة:
1. استخدم زر "تعديل الجلسة"
2. غيّر النوع
3. أدخل الرابط أو الموقع حسب النوع الجديد
4. احفظ التعديلات
```

---

### **3. التعديلات السريعة**

```
للتعديلات السريعة (رابط فقط):
✅ استخدم زر "تحديث رابط المقابلة" 🎥

للتعديلات الشاملة:
✅ استخدم زر "تعديل الجلسة" ✏️
```

---

## 📁 الملفات المُعدّلة

### **مُعدّل:**
- ✅ `admin/js/interview-sessions-manager.js`
  - إضافة دالة `editSession()`
  - إضافة دالة `updateMeetingLink()`
  - تحديث `renderSessions()` لإضافة الأزرار
  - تحديث `window.interviewSessionsManager` للتصدير

---

## ✨ الخلاصة

تم إضافة ميزتين مهمتين لنظام إدارة جلسات المقابلات:

1. ✅ **تعديل الجلسة** - تعديل جميع بيانات الجلسة بسهولة
2. ✅ **تحديث رابط المقابلة** - تحديث سريع لروابط المقابلات الأونلاين

**الفوائد:**
- ⚡ سرعة في التحديث
- 🔒 أمان في التحقق من البيانات
- 🎯 سهولة في الاستخدام
- 📱 واجهة بديهية

**النظام جاهز للاستخدام!** 🎉
